# M24-05 release evidence

This directory freezes the evaluator, locked benchmark, scoped branch coverage,
release verifier, and package records for the provisional M24-05 subgroup equity
evaluator. Values are generated from committed source and must be verified with
`tools/verify_m2405_release.py` before publication.

The M24-04 relationship is media-only and caller-declared. No unpublished
upstream runtime ABI is imported. The implementation emits subgroup equity and
review material, not a biomarker panel or biological truth claim.
