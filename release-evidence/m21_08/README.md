# M21-08 release evidence

This directory is the machine-readable release record for provisional
`GLIO-PROTEOGEN-M21-08`. `evaluation.json` and `benchmark.json` are generated
from the frozen evaluator and locked benchmark wrapper. `coverage.json`
records branch-enabled scoped coverage. `package.json` records wheel/sdist
hashes, sizes, member counts, and isolated import verification.

Authority: `sha256:0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`,
`GLIO-PROTEOGEN_240_Module_Dossier.md:7544-7584`.

The independent verifier is `tools/verify_m2108_release.py`. It requires all
manifest checks to pass and is intended to run after generated build/install
directories are removed.
