import fixture from "./proteogenomic-state.json";

export const algorithmProfile = fixture.profile;
export const demoRequest = fixture.request;
export const analysisResult = fixture.result;
export const verificationResult = fixture.verification;
