# M01-01 traceability matrix

| ID | Requirement | Contract / algorithm | Executable evidence | Gate |
| --- | --- | --- | --- | --- |
| M01-01-R01 | Reject coercion, unknown properties, and ambiguous duplicate JSON keys | strict JSON decoder; `FrozenModel`; request and field contracts | `coerced_*`, `extra_unknown`, and `duplicate_key` fixtures; API/CLI/plugin parser parity | G0 |
| M01-01-R02 | Distinguish absent, null, and explicit unresolved states | `MetadataValue`; field missingness allow-list | `absent_required`, `null_required`, conformant unresolved fixture; property tests | G0 |
| M01-01-R03 | Reject NaN and infinity | strict JSON loader; `allow_inf_nan=False`; canonical serializer | `nan`, `infinity` fixtures; contract and property tests | G0 |
| M01-01-R04 | Keep behavior and content identity independent of semantically unordered field, entry, and value order | domain canonicalization; deterministic issue sort | digest and validation metamorphic property tests/eval | G0 |
| M01-01-R05 | Enforce schema, assay, specimen, controlled-vocabulary, unit, mandatory-field, identity-key, and cardinality compatibility | exact-version validation; indexed field graph; pinned UCUM 2.2 dimensions and Decimal affine conversion into explicit reference units | invalid-code/alias/dimension and cross-unit bound/predicate regressions; validator eval cases | G0 |
| M01-01-R06 | Apply declared compatibility-rule action | indexed predicate evaluator | `evaluate_quarantine`; `compatibility.requirement_failed` assertion | G0 |
| M01-01-R07 | Never convert missing evidence to a negative finding | unresolved-value union; typed issues | `evaluate_reject`; missingness policy/property assertions | G0 |
| M01-01-R08 | Preserve immutable upstream controls and fail closed on non-authorizing decisions | frozen typed decisions; generic `accepted`, lineage `resolved`; canonical identity-key binding | immutability snapshot; pre-hash/no-event authorization matrix; lineage-mismatch quarantine test | G0 |
| M01-01-R09 | Respect consent without inferring it | supplied `ConsentState`; granted-before-hash gate; decision precedence | consent policy matrix and revoked-before-hash/no-event tests | G0 |
| M01-01-R10 | Emit only a versioned schema receipt or conformance profile, each with support, seven-dimension uncertainty, provenance, evidence, and limitations | discriminated `M0101Output`; shared typed evidence envelope; exactly seven privacy-minimized control-decision records | output-envelope/forgery tests, provenance persistence/reconstruction, schema snapshot, forbidden-output, and policy tests | G0 |
| M01-01-R11 | Exclude kinase state, all-omics fusion, and treatment recommendation | descriptor prohibition list and manifest ceiling | ownership policy test | G0 |
| M01-01-R12 | Make registration and evaluation auditable and replay-safe | canonical request/event digests; append-only chain | service replay, conflict, persistence, and chain tests | G0 |
| M01-01-R13 | Export stable, tool-valid public contracts and detect drift | eight self-identifying JSON Schema Draft 2020-12 documents with versioned URNs, standard expressible invariants, documented runtime extensions, and canonical digests | dialect meta-validation, standard-schema/runtime fixture parity, extension-key assertions, and `tests/snapshots/m01_01/schema_digests.json` | G0 |
| M01-01-R14 | Meet bounded validation cost without regex backtracking exposure | linear indexing/predicate evaluation plus per-entry `O(n_i log n_i)` canonical value ordering; exact-repeat-only regex subset rejected before compilation | adjacent-optionals regression, order-invariance checks, microbenchmarks, and eval latency distribution | G0 |
| M01-01-R15 | Keep evidence non-clinical | synthetic fixture policy | closed fixture manifest and lexical policy test | G0 |
| M01-01-R16 | Bound parser, scalar, string, and every public collection | 4 MiB ASGI byte gate; 128-character semantic versions; typed scalar and tuple caps; 998 client limitations plus two mandatory output slots; exactly seven control decisions | recursive cap-schema audit; first-excess-character/item tests; header, streamed API, plugin, and CLI byte-limit tests | G0 |
| M01-01-R17 | Keep the G0 implementation deterministic and free of unevaluated, trained, or clinical-model behavior | quarantine-first rules; versioned deep-frozen synthetic nearest-medoid consensus guard with abstention; canonical artifact pins and bounded reads; coarse public status with exact proximity values kept internal; no fitting, calibration, runtime fetch, external dataset, clinical claim, or promotion path | closed data/model/reference inventory; executable in-domain/OOD/abstention/order/privacy evals; consensus microbenchmark; tamper, immutability, no-leak, replay, and fail-closed service tests; policy test | G0 |

## Evidence commands

```bash
uv run pytest tests/contract tests/property tests/policy tests/integration
uv run python -m evals.m01_01.run
uv run pytest benchmarks/m01_01_validation.py --benchmark-only --no-cov
```

Every row has executable evidence. A requirement is not considered implemented merely
because a contract type or documentation paragraph exists.

The dossier-level artifact inventory, risk controls, recovery/rollback protocol, and
non-self-issued reviewer checkpoint are recorded in
[`docs/evidence/M01-01.md`](../evidence/M01-01.md).
