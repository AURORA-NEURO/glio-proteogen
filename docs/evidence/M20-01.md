# M20-01 release evidence

## Authority

This lane is traceable to dossier SHA-256
`0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`, exact lines
`6876-6916`. The ABI is `0.1.0-provisional` pending ML engineering owner confirmation.
M20-01 owns typed upstream discovery and compatibility beneath Biomarker-panel translation,
supports the parent target `protein subtype`, and does not own kinase state, generic
all-omics fusion, direct treatment recommendation, identity or consent inference, or
unsupported negative findings.

## Granular implementation chain

| Stage | Commit | Evidence |
| --- | --- | --- |
| contract closure | `cead8f5`, `7cdfc65` | typed candidates, closed compatibility buckets, consent/support/provenance closure, canonical digest identity |
| deterministic runtime/service/plugin | `0748eb2` | seven-control preflight, deterministic compatibility resolution, safe abstention, replay/tamper verification |
| API/CLI/plugin interfaces | `ddc80f5`, `0c6dc8dc`, `e3937544` | strict parse-once JSON, standalone and central FastAPI/Typer parity, sanitized errors, no-overwrite behavior |
| evaluator/benchmark/fixtures | `da631bb` | eight frozen scenarios, eight adversarial oracles, 10-iteration bounded benchmark |
| adversarial coverage | `38d8df8` | 24 focused tests, branch-enabled scoped coverage at or above 95% |
| documentation/traceability | this evidence record | module manifest, authority record, requirement matrix, release verifier |
| release verifier | `153cd4d` | strict evaluation/benchmark/coverage/package evidence validation |
| package/release evidence | current audit | fresh two-build wheel/sdist hashes, isolated import, evaluation/benchmark/coverage verification |

## Gate record

Ruff and MyPy strict are clean for the M20-01 source/evaluator/test set and the central
API/CLI files; focused compileall and isolated-wheel import pass. The evaluator passes
8/8 scenarios and 8/8 adversarial cases. The 10-call benchmark is recorded in
`docs/evidence/m20_01/benchmark.json` against provisional 500,000,000 ns and 750,000,000
ns budgets: mean 1,713,630 ns and p95 1,770,500 ns. Scoped branch-enabled coverage for
the M20-owned source/evaluator surface is 96% (577 statements, 132 branches; fail-under
95). Central API/CLI registration is covered by the interface parity regression.
The fresh wheel is 3,669,139 bytes with SHA-256
`3c437c20762ebc11a86cbc17096261ec878dba5066128cb169f67cd68591521c` (1,913 members);
the fresh sdist is 4,211,022 bytes with SHA-256
`0fe3c735e25cd1199604124edaaca8d4ad4f85dc2d9af5d33665c15b91623a6d`. The isolated
import check loaded `glio_proteogen.contracts.m20_01.M2001_MODULE_ID` successfully.

This is an engineering-provisional lane eligible for human review, not automatic clinical
promotion. Upstream evidence is referenced rather than traversed or mutated; unsupported
and unknown candidates remain typed and auditable. Provenance input digests include each
candidate's optional provenance artifact, so the artifact supporting a compatible candidate
cannot remain outside the replay-visible provenance closure.
