# M06-07 release evidence

This evidence is local-only and describes the provisional ABI. It must not be
treated as an owner-approved public interface until the dossier handoff is
frozen.

## Gates

- Dossier SHA and exact slice verified: SHA-256
  `0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`, lines
  2100–2143.
- Focused contract/runtime/interface/evaluator suite: 36 passed.
- Ruff: clean for contracts, runtime, evaluator, benchmark, and focused tests.
- MyPy strict: clean across 15 checked files.
- Branch coverage: 99% overall (538 statements, 2 uncovered CLI lines and 1
  partial branch are the Typer exception/module-entry paths).
- Evaluator: calibrated path, upstream abstention, coverage abstention,
  selected estimate, prediction set, deterministic replay all pass.
- Benchmark: 10 iterations, 7,681,300 ns mean and 8,593,700 ns p95, under
  2,000,000,000 ns mean / 3,000,000,000 ns p95 budgets.
- Package/import evidence: `M06-07-package.json`.

## Negative-path coverage

The suite covers withheld consent, unresolved identity, rejected controls,
missing calibration metrics, outside-envelope coverage, excessive calibration
error, unsupported upstream uncertainty, malformed JSON, duplicate keys,
unknown schema, non-canonical replay bytes, digest mismatch, unsafe result
closures, duplicate diagnostics, unbound prediction sets, CLI output
overwrite, and plugin execution without a validation token.

## Local commits

1. `ecdf232` — contract/runtime hardening.
2. `25c1fdd` — plugin/API/CLI parse-once parity.
3. `b371839` — evaluator fixtures and benchmark.
4. `16ce1ab` — adversarial coverage and replay branches.
5. `90b1d36` — strict type gates for adversarial fixtures.
