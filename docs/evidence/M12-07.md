# M12-07 release evidence

Authority: dossier SHA-256 `0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`, lines 4260–4303.

## Evidence matrix

| Gate | Evidence | Result |
|---|---|---|
| G0 contract/schema | six strict JSON Schema 2020-12 exports; request/result replay closure; duplicate and wrong-media rejection | Passed |
| G1 functional | deterministic six-control adjudication; high-grade supported path; hard direction failure; conflict preservation | Passed |
| G3 robustness | missing, unsupported, failed and abstained controls; denied quality gate; strict duplicate-key/non-finite JSON rejection | Passed |
| G3 uncertainty/abstention | seven uncertainty dimensions; explicit abstention; human-review closure; no unsupported-to-negative conversion | Passed |
| G4 safety/recovery | seven upstream preflight gates, identity/context binding, consent gate, provenance records, canonical replay/tamper verification | Passed |

## Locked evaluator

Fixture: `tests/fixtures/m12_07/scenarios.json`.

Expected fixture digest: `sha256:2b429822195cdfcd881ea1900d6acdb880bf57679e9c6f62079cbd65cea91e1b`.

Cases: `supported_high`, `failed_control`, `missing_observation`,
`direction_mismatch`, `unresolved_conflict`, `denied_quality_gate`,
`abstained_control`, `replay_tamper`.

The evaluator executed 8/8 cases and passed. The 10-iteration benchmark passed
with mean `2,517,360 ns`, p50 `2,421,850 ns`, p95 `2,839,500 ns`, maximum
`2,839,500 ns`, below provisional `2,000,000,000 ns` mean and
`3,000,000,000 ns` p95 budgets.

Scoped branch-enabled coverage over M12-07 contract, runtime/plugin/service
and standalone adapter is 480/482 statements (99.29%) and 78/80 branches
(97.50%), above the 95% release floor. Two replay-verifier defensive branches
are not reachable after the contract's own digest closure and remain visible
in the coverage report.

## Safety boundary

The module does not inspect or authenticate artifact payloads. Caller-declared
observations are labeled as such; failed or absent declarations cause safe
abstention. The module never owns KINOPHOS kinase state, generic all-omics
fusion, identity inference or treatment recommendation.
