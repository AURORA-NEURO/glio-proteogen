# M06-06 evidence record

This is a local-only provisional evidence record for
`GLIO-PROTEOGEN-M06-06`. The implementation is based on the permitted dossier
behavioral brief and scaffold checkpoint `794ea5a`; no quarantined dossier
path was inspected. No owner-confirmed ABI exists yet.

## Evidence classes

The evaluator creates a complete, content-digested synthetic M06-05 contract
result to exercise M06-06 replay binding. It is explicitly a contract-level
upstream builder, not an execution of the M06-05 estimator. Scenarios cover
integrated upstream data (M06-06 abstains for calibration lock) and abstained
upstream data (M06-06 remains unsupported/review-safe).

The interface suite covers all seven schema routes, Draft 2020-12 schema
validity, strict JSON and media rejection, seven-control authorization, stale
upstream request/result digests, forged/copy plugin tokens, API parity, CLI
parity, and no-output-on-failure behavior. The benchmark runs one warmup and
exactly 25 measured calls, requiring deterministic result digests and the
provisional 500 ms mean / 750 ms p95 budgets.

## Interpretation limits

The current output is abstention evidence only. It does not demonstrate
calibrated uncertainty, interval coverage, model-form estimation, or a
production-ready upstream handoff. Owner review must freeze the ABI and
calibration artifact before release scope is considered.
