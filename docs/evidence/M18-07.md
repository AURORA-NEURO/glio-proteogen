# M18-07 evidence inventory

This G3 package verifies the bounded downstream typed export beneath the Spatial proteomics
projection. It consumes caller-declared biomarker-panel references and never authenticates or
dereferences upstream content, infers identity/consent, erases disagreement, emits unsupported
negatives, performs generic all-omics fusion, enters KINOPHOS/kinase scope, or recommends
treatment.

| Evidence | Artifact | Verification |
| --- | --- | --- |
| Authority-bound contract | `src/glio_proteogen/contracts/m18_07/` | dossier SHA/slice, parent, ownership, compatibility and signed-envelope closure |
| Deterministic runtime | `src/glio_proteogen/modules/c18_spatial_proteomics_projection/m18_07_downstream_typed_export/` | seven-control preflight, typed output/abstention, seven uncertainty dimensions, replay/tamper |
| Strict interfaces | `src/glio_proteogen/adapters/m1807.py` | FastAPI/Typer/plugin parse-once, sanitized errors, no-overwrite |
| Locked scenario corpus | `tests/fixtures/m18_07/scenarios.json` | eight scenarios, authority metadata, SHA-256 `c78c68b9617939d55548df4d0c3401dc35c39da57326de57f0572df92dbee2a4` |
| Executable evaluator | `evals/m18_07/run.py` and `evals/m18_07/evaluation.json` | 8/8 scenarios and 8/8 adversarial checks |
| Microbenchmark | `evals/m18_07/benchmark.py` and `evals/m18_07/benchmark.json` | ten deterministic export calls, 500 ms / 750 ms provisional budgets |
| Scoped coverage | `release-evidence/m18_07/coverage.json` | branch-enabled 505/509 statements, 86/86 branches, fail-under 95 |
| Traceability | `docs/traceability/GLIO-PROTEOGEN-M18-07.csv` | dossier requirements mapped to implementation and evidence |
| Release verifier | `tools/verify_m1807_release.py` and `tests/tooling/test_m1807_release.py` | evaluation, benchmark, coverage, fixture, package and tamper evidence |

## Granular commit and gate record

| Stage | Commit | Evidence |
| --- | --- | --- |
| contract/replay closure | `809671f` | unique identifiers, ownership/compatibility, signed result and canonical digest contract |
| deterministic runtime/service/plugin | `aeccb31` | seven-control fail-closed preflight, bounded export/abstention, uncertainty/provenance/evidence |
| strict API/CLI/plugin interfaces | `109e20c` | FastAPI/Typer parity, strict parse-once validation, sanitized failures and no-overwrite |
| evaluator/benchmark/fixtures | `da9ba1b` | eight frozen scenarios and ten-call benchmark wrapper |
| adversarial/replay coverage | `0dab19c` | replay/tamper, consent/privacy, forbidden-boundary, invalid-result and service-path tests |
| strict typing/format closure | `bacb8de`, `777346c` | Ruff and strict MyPy cleanup across runtime/evaluator/interface files |
| authority/docs/traceability | `9ee9ec5` | exact dossier hash/slice, G3 boundary and requirement matrix |
| release verifier/evidence | `ccb7e96` | frozen evaluator/benchmark/coverage JSON, fixture digest and verifier tamper tests |
| package/release evidence | pending | wheel/sdist hashes, member counts, isolated import and final verifier result |

## Final local gate record

- Focused functional/evaluator suite: 23 tests pass; release verifier suite: 4 tests pass.
- Ruff check and format pass. Strict MyPy passes across 15 runtime/interface/evaluator/test
  files, plus the release verifier/tooling tests.
- Scoped branch-enabled coverage: 509 statements, 505 covered (99.21414538310412%); 86
  branches, 86 covered (100%); fail-under 95.
- Evaluator: 8/8 scenarios and 8/8 adversarial cases (100%, target 100%).
- Locked ten-call benchmark: mean 2,364,580 ns, p50 2,361,950 ns, p95 2,610,600 ns, against
  500,000,000 ns mean and 750,000,000 ns p95 budgets.

The benchmark is a software regression tripwire, not scientific validation. Package hashes and
isolated import are recorded only after the final wheel/sdist build and release verifier pass;
generated `build/`, `dist-*`, install-target and coverage files are removed after exact-path
verification.
