# M04-08 current-base release evidence

Authority was verified against dossier SHA-256
`0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`, lines `1424-1464`.
This lane is based on `8555ddfb` and binds the exact public M04-07 result contract. It is a
release-packaging/provenance implementation only: it does not infer protein, proteoform, isoform,
glioma, kinase, clinical, or treatment biology.

The implementation adds 6,401 scoped Python lines across 23 files: 3,479 source, 2,407 tests, and
515 evaluator/benchmark lines. Relative to the current base tree, tracked Python totals move from
538,304 to 543,668 lines (+5,364 net). The focused current-base suite is 96/96, the locked evaluator is 12/12,
and the 10-call benchmark passes at 148,554,680 ns mean, 133,212,150 ns median, and 222,630,700 ns
p95 against 2,000,000,000/3,000,000,000 ns budgets. Ruff, formatting, compileall, and strict MyPy
are clean across 22 files.

Two pinned wheel/sdist builds are byte-identical. The wheel is 3,663,225 bytes with SHA-256
`c78421a07583efbb710a30a133801f942b3828bc15712945362d1e6c05dacb75`; the sdist is 4,199,063 bytes
with SHA-256 `3635380cecf8a928e2e3ad65778c19a24d26265e146c338a66e71cc406ce2348`. An isolated wheel
installation imported `glio_proteogen` and `M0408_MODULE_ID` successfully.

Coverage is recorded in `release-evidence/m04_08/coverage.json` using the complete current-base
contract/runtime/evaluator scope and evaluator append. Statement coverage is 98.4079% (1,298/1,319),
and the branch-enabled combined percentage is 97.9077% (293/306 branches, 11 partial), including
95.7516% branch coverage. The engine is 95.0%, kernel/contract/schema/plugin/service are 100% at
the component level, and the remaining misses are defensive v1/engine edges. This scoped result
clears the repository-wide branch fail-under-95 threshold for the M04-08 source/evaluator policy.

The JSON receipts in this directory are the authoritative run outputs for evaluation, benchmark,
coverage, and package identity. Generated build/install/coverage directories are intentionally
excluded from the commit.
