# M19-08 evidence inventory

This G5 package verifies a deterministic translation-health monitoring and rollback
boundary over synthetic, caller-declared metadata. It does not authenticate an issuer,
validate telemetry truth, approve a rollback, or establish a clinical or biological claim.

| Evidence | Artifact | Verification |
| --- | --- | --- |
| Authority handoff | `GLIO-PROTEOGEN_240_Module_Dossier.md:6824-6864` | SHA-256 `0a6b200c...da181`; exact M19-08 responsibility, owner, parent, safety class, and prohibited authority |
| Strict contract | `src/glio_proteogen/contracts/m19_08/v1.py` | finite measurements; closed states and decisions; unique source, observation, discrepancy, and finding identities; source/evidence closure; request/result digest closure |
| Deterministic runtime | `.../m19_08_translation_monitoring_service/engine.py` | seven-control preflight; telemetry/support/workflow/discrepancy classification; safe abstention; explicit uncertainty; canonical replay and tamper rejection |
| Service and plugin | `service.py`, `plugin.py` | strict parse-once JSON; bytes/mapping boundaries; instance-bound plugin token; execute, verify, and replay parity |
| API and CLI | `src/glio_proteogen/adapters/api.py`, `cli.py` | schema export, strict validation, M19-08 operation routing, sanitized failures, and canonical parity; central denied-consent regression is 403 with no traceback |
| Central transport inventory | `tests/integration/test_central_late_module_surfaces.py` | canonical `create_app` route and `m1908-translation-health` group are exercised alongside the other late-module surfaces, including malformed-request rejection |
| Locked fixture | `tests/fixtures/m19_08/scenarios.json` | eight scenario names, eight adversarial cases, dossier-bound metadata, and 95 percent target |
| Executable evaluator | `evals/m19_08/run.py` | 8/8 scenarios, 8/8 adversarial checks, support/control/replay/rollback oracles |
| Benchmark | `evals/m19_08/benchmark.py` | 25 timed deterministic engine calls after one warm-up; mean/p95 budgets 500,000,000 / 750,000,000 ns |
| Focused test suite | `tests/contract`, `tests/runtime`, `tests/integration`, `tests/evaluation` | 48 passing tests, including nested evidence provenance, seven caller-claim surfaces, API/CLI/service parity, duplicate identity, forged digest, replay mismatch, invalid JSON, token, and state/decision cases |
| Coverage | scoped `coverage run --branch` report | 96.2171 percent combined branch-enabled coverage over 514 statements and 94 branches (499/514 statements, 86/94 branches); generated coverage artifacts are not release inputs |
| Claims ceiling | `M1908_PROHIBITED_CLAIM_TERMS`, `prohibited_claim_boundary` | caller-controlled kinase/all-omics, treatment, identity/consent, mutation, protein/proteoform/isoform, glioma-specific, clinical/biological, and relabeling claims abstain before a health report is constructed |
| Release verifier | `tools/verify_m1908_release.py` | evaluation, benchmark, branch coverage, reproducible package hashes, and isolated import verified |
| Traceability | `docs/traceability/GLIO-PROTEOGEN-M19-08.csv` | dossier responsibilities, safety controls, replay, interfaces, evaluation, benchmark, and recovery mapped to executable evidence |

## Locked risk controls

- Seven caller controls are authorized before request traversal. Denied consent,
  malformed context, or unsupported upstream media fails closed.
- The upstream binding is media-type and artifact based. M19-08 does not invent or
  authenticate an M19-07 service and does not replace a typed export with a receipt.
- Telemetry, support drift, workflow effects, and discrepancies retain their declared
  status. `FAIL`, `NOT_EVALUABLE`, and unresolved discrepancy are not silently treated
  as healthy or negative biological evidence.
- Critical failures use the locked rollback threshold. Suspension and rollback are
  explicit state/decision pairs; each state has exactly one permitted decision.
- Unsupported or insufficient support returns `ABSTAINED` without a health report and
  with an explicit abstention reason. The result remains replayable and reviewable.
- Every observation and discrepancy ID is unique across categories. Finding IDs and
  finding codes are unique, preventing duplicate evidence from changing precedence.
- Result digests bind canonical request and result content. Identifier forgery,
  recomputed-payload forgery, replay mismatch, and invalid digest are rejected.
- Provenance input digests bind every emitted evidence artifact, including all seven
  control evidence references, so the evidence projection cannot outrun provenance.
- Seven uncertainty dimensions remain explicit and not estimable. Runtime latency is
  not evidence of monitoring accuracy or rollback effectiveness.
- API, CLI, service, plugin, and library boundaries share strict reconstruction and
  canonical result semantics. Duplicate keys and oversized JSON fail before execution.
- Rollback is a decision record only. Recovery requires external governance and a new
  immutable superseding result; no previous result or evidence is mutated or erased.

## Evaluation and benchmark record

The evaluator executes the locked healthy, degraded, rollback-required, suspended,
not-evaluable, denied-control, rejected-upstream, and replay-tamper scenarios. It also
checks critical telemetry/support/workflow paths, unresolved discrepancies, explicit
seven-dimensional uncertainty, and parent-output suppression. All 8 scenario oracles
and all 8 adversarial checks passed.

The benchmark constructs the request outside timing, performs one warm-up, and times
25 public `M1908TranslationMonitoringEngine.infer` calls. The observed sample was
1,719,444 ns mean and 1,945,100 ns p95 on the local environment, below the provisional
500,000,000 ns and 750,000,000 ns budgets. It is a software-regression tripwire, not a
performance claim about production telemetry or deployment infrastructure.

## Review and recovery

The implementation cannot self-approve a release, authenticate a signer, or promote a
rollback. A governed reviewer must independently review the exact source commit,
fixture/evaluator digest, result/replay evidence, support status, and rollback policy.
For critical drift or unresolved discrepancy, preserve the exact immutable request and
result, suspend downstream use, obtain external review, and publish only a new
superseding request after the source policy or upstream export is corrected. Never
overwrite, relabel, deduplicate, or erase the prior evidence.
