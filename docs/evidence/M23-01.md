# GLIO-PROTEOGEN M23-01 evidence

M23-01 is a provisional reference-truth and benchmark curator beneath the
variant-peptide parent. It locks caller-declared reference and control
material only when the inclusion, leakage-audit, adjudication, challenge-set,
provenance, evidence, and configuration declarations are complete. It emits
an immutable benchmark package or an explicit review-required abstention; it
does not infer biological truth, turn unsupported material into a negative,
authenticate an issuer, or emit a variant-peptide estimate.

Authority is the permitted dossier SHA-256
`sha256:0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`,
exact slice `GLIO-PROTEOGEN_240_Module_Dossier.md:7956-7996`. The ABI remains
`0.1.0-provisional`; ownership is Data engineering, safety class S3, and gate
G0. The implementation keeps all upstream material caller-declared and does
not invent a service import or issuer authority.

The contract closes request identity, reference/control partitions, exact
source-artifact binding, inclusion and adjudication coverage, challenge-set
flags, package lock digest, result identity, and replay digest. Runtime
preflight requires accepted configuration/provenance/quality/support/intended
use controls, resolved identity, and granted consent. Pending, reviewed, or
rejected-included adjudications and missing challenge material fail closed to
review-required abstention with findings. Every result carries typed seven
dimension uncertainty, provenance, evidence, limitations, and human-review
semantics.

Strict FastAPI, Typer, and parse-once plugin interfaces expose validation,
curation, schema, and replay verification with sanitized errors and no
overwrite behavior. The frozen evaluator covers eight scenarios, including
locked success, adjudication abstention, missing challenge material,
authorization, source substitution, replay tamper/determinism, and plugin
token binding.

Release records are in `release-evidence/m23_01/` and are verified by
`tools/verify_m2301_release.py`.
