# M05-01 evidence inventory

| Evidence | Path / command | Locked assertion |
|---|---|---|
| Dossier source | authoritative attachment SHA-256 `0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`, lines 1476–1516 | exact module responsibility, boundary, inputs/outputs, controls, uncertainty, review, acceptance, owner/safety/gate, architecture options, and future gates |
| Public contracts | `src/glio_proteogen/contracts/m05_01` | strict immutable v1 schema/profile/request/result and full replay helpers |
| Runtime | `src/glio_proteogen/modules/c05_ptm_localization/m05_01_protocol_metadata` | preflight-first sealed deterministic conformance only |
| Genuine builder | `evals/m05_01/run.py::build_scenario_request` | creates strict protocol/profile/context without handwritten results or upstream content |
| Locked corpus | `tests/fixtures/m05_01/scenarios.json` | exactly 8 groups × 5 unique cases = 40 |
| Executable eval | `python -m evals.m05_01.run` | declared=executed=passed=40, zero missing/duplicate cases |
| Benchmark | `python -m evals.m05_01.benchmark` | one maximum-shape warm-up + exactly 25 public calls, mean ≤2 s and p95 ≤3 s |
| Focused tests | M05-01 contract/lifecycle/interface tests | strict schema, authorization firewall, abstention/quarantine, replay, forgery, privacy, recovery, and parity |
| Traceability | `docs/traceability/GLIO-PROTEOGEN-M05-01.csv` | dossier and locked-risk requirements mapped to implementation and evidence |
| Data/model/reference manifest | `docs/modules/M05-01.manifest.md` | synthetic metadata only, zero models, opaque references, no fetch/parse |
| Release verifier | `tools/verify_release_artifacts.py m05-01-evidence` | exact corpus, maximum shape, iterations, warm-up, and timing budget closure |

## Acceptance evidence

The contract exposes exactly 13 JSON Schema 2020-12 documents. All mandatory identity, version,
unit, cardinality, compatibility, unresolved, and parent-handoff declarations are explicit; there is
no undocumented optional scientific payload. Strict JSON rejects duplicate keys, coercion, unknown
members, non-finite values, and request bodies above 4 MiB.

Every valid result embeds the exact request and deterministically rederives all identifiers, digests,
findings, status, disposition, receipt, support, uncertainty, provenance, evidence, limitations,
review state, and time. Re-signed derived-region substitutions reject. Semantic-set reordering
retains complete request/result equality.

Authorization tests deny each of seven controls without touching protocol/profile traps. Dict-family
access uses built-in operations after exact-key and size scans; arbitrary mappings and accessors are
not invoked. Ordinary exceptions are sanitized and `BaseException` propagates. Plugin tokens require
issuance registry membership, original request identity, and exact byte snapshot.

## G0 and future-gate interpretation

- G0: the locked corpus and strict tests prove every declared critical case and zero silent coercion.
- G1: no primary scientific error claim is made; only metadata invariants are installed.
- G3 robustness: reviewed metadata reordering stays invariant; unsupported/novel/OOD configurations
  abstain rather than becoming negative.
- G3 uncertainty: no probability is emitted. Every uncertainty dimension is non-estimable and the
  support domain is explicitly narrowed, satisfying the stated alternative to 85–95% coverage.
- G4: ownership flags are false, provenance is exact, and superseding recovery is append-only.

## Reproduction

```text
uv run python -m evals.m05_01.run --output m05-01-eval.json
uv run python -m evals.m05_01.benchmark --output m05-01-benchmark.json
uv run python tools/verify_release_artifacts.py m05-01-evidence m05-01-eval.json m05-01-benchmark.json
```

These artifacts do not constitute reviewer approval or scientific/clinical qualification.

## Current-stack gate record

This stack was transplanted additively onto merged base `956e958b` (M04-05 current-base
reintegration). The local M05-01 sequence is `28393d89` (contracts), `1c825684`
(runtime/API/CLI/plugin), `6931f7ee` (interface strictness), `8b82066e` (evaluator, fixture,
benchmark, verifier, and release workflow), and `9365e608` (branch-hardening tests). The branch
is intentionally not published until the M04-06 shared-route base is available.

The focused M05-01 contract/lifecycle/interface/verifier matrix passed **111 tests**. Ruff check
and format passed; strict MyPy passed for 18 targeted source/eval/test/tool files; and compileall
passed. The executable evaluator passed 40/40 cases across eight groups of five, with fixture
SHA-256 `1e5650e2595a7ee4ee37a27da0ecba232802cf8c4b7c584a6dfe169f8a25f2f8`. The 25-call benchmark
passed with request/result sizes 50,014/66,750 bytes, mean 258,096,620 ns, p50 277,325,100 ns,
p95 339,827,200 ns, and maximum 371,267,900 ns against 2,000,000,000/3,000,000,000 ns budgets;
the report digests were `a87304d4b92a55b066c14a9083263bba1634ac49a9478e816eff932e350e5901` and
`eb8d9aa89fbba056fe9a26af834046678dc0b2496eee4f46a8253201ddb7dcfc`. Scoped branch-enabled
coverage was **98.69678540399653%** (1,001 statements, 992 covered; 150 branches, 144 covered,
6 partial) with fail-under 95. Generated reports were removed after recording these values.
