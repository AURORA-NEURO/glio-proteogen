# M28-04 release evidence

This record binds the M28-04 API/SDK/CLI gateway implementation to its provisional authority
slice and machine-readable evidence under `docs/evidence/m28_04/`.

## Authority and boundaries

- Dossier SHA-256: `0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`.
- Exact authority slice: lines `9888-9928`.
- ABI remains `0.1.0-provisional`; no frozen endpoint, media type, owner confirmation, or
  scientific catalogue is inferred.
- This module exposes caller-declared access metadata only. It cannot infer protein, proteoform,
  isoform, glioma-specific biology, kinase activity, treatment, identity, consent, or generic
  all-omics conclusions.

## Evidence index

| Record | Result |
| --- | --- |
| `evaluation.json` | 10 declared checks, 10 passed; fixture digest `sha256:f3a681830908eaf1ab86163b4c002d5a287dd9363ff3cd8710503c9af8fd9c22` |
| `benchmark.json` | 10 iterations; mean `2,113,360 ns`; p95 `2,663,800 ns`; budgets `500,000,000/750,000,000 ns`; passed |
| `coverage.json` | Branch-enabled `95.25222551928783%`; 963/1011 weighted units; fail-under 95; passed |
| `package.json` | Wheel/sdist hashes, sizes, member counts, and isolated import evidence |
| `release.json` | Authority, commit chain, LOC, and gate cross-reference |

`tools/verify_m2804_evidence.py` validates cross-record module identity, fixture/result binding,
benchmark budgets, weighted coverage, package hashes, and release status. The focused tooling test
executes that verifier.

## Replay closure

- [x] Published access surfaces bind exact request operations/media,
  authorizations, idempotency records, jobs, compatibility, errors, audit,
  configuration, surface identity, and version; rehashed mutations are rejected.

## Granular implementation commits

1. `26d39655` — contract cross-reference and replay identity closure.
2. `b4949000` — deterministic runtime/service/plugin and replay implementation.
3. `7ba0e71e` — contract formatting normalization.
4. `4da9424a` — strict FastAPI, Typer CLI, SDK, and parity interfaces.
5. `c7063d0d` — request/result byte-limit hardening.
6. `03557340` — evaluator fixture and benchmark matrix.
7. `570b918b` — adversarial cross-reference/replay coverage.
8. `e13e82e0` — evaluator and deep adversarial test closure.
9. `677ec1fc` — static, typing, evaluator, and validator gate normalization.
