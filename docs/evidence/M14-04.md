# M14-04 release evidence

Authority is dossier SHA-256
`0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`, exact
lines `4848–4888`; ABI remains `0.1.0-provisional`.

The seven-case evaluator covers posterior and state inference, explicit and
unknown-method abstention, invalid bounds, replay/tamper detection, and denied
control authorization. The adversarial matrix also covers opaque-input
preflight, counter-evidence closure, canonical determinism, strict JSON,
sanitized API/CLI errors, and no-overwrite behavior.

The typed research lane now consumes explicit protein/PTM observations mapped to
five glioma signaling programs. It uses damped Huber coordinate descent with
signed network coupling, deterministic bootstrap intervals, evidence counts,
top drivers, and topology ablation effects. Missing and unsupported observations
are excluded; an all-unsupported request abstains. The original posterior/state
method grammar remains compatibility-only, and typed outputs are review-gated
research scores rather than causal, clinical, prognostic, or treatment claims.

The historical release record remains in `release-evidence/m14_04/`. The current
contract/runtime suite executes 29 tests; the legacy 10-iteration benchmark
remains within its provisional 2e9/3e9 ns mean/p95 budgets.
