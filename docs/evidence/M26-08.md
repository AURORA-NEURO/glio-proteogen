# M26-08 release evidence

| Field | Evidence |
| --- | --- |
| Module | `GLIO-PROTEOGEN-M26-08` |
| Authority | SHA-256 `0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`; `GLIO-PROTEOGEN_240_Module_Dossier.md:9344-9384` |
| Owner / safety / gate | Scientific engineering / S3 / G5 |
| Parent | protein subtype |
| Contract | `0.1.0-provisional`; no frozen ABI or issuer authority is claimed |
| Python contribution | 2,480 added / 21 deleted / 2,459 net lines from scaffold `0c751ec2`; breakdown in `release-evidence/m26_08/loc.json` |
| Focused tests | 67 passed across contract, runtime, interface, evaluator, adversarial, and release-verifier suites |
| Scoped coverage | Branch-enabled combined 98.54111405835544%; 648 statements (642 covered), 106 branches (101 covered), fail-under 95; details in `release-evidence/m26_08/coverage.json` |
| Evaluator | 10/10 frozen scenarios passed; fixture digest and per-case checks are in `release-evidence/m26_08/evaluation.json` |
| Benchmark | 10 calls; mean 1,976,310 ns; median 1,867,200 ns; p95 2,856,100 ns; budgets 500 ms / 750 ms |
| Package | wheel 3,663,225 bytes, SHA-256 `b399a56dccf2395d16d9d64c71459f2ce8571eaa3b2ab6aedb6d6e2808011cb8`; sdist 4,201,147 bytes, SHA-256 `ac603171280ecd573e8b234c8cebca3277047c01b1b8aaefab6eef1c6672a996`; two-build byte identity with `SOURCE_DATE_EPOCH=315532800`, isolated import and artifact-bound release verifier passed |

## Review checklist

- [x] Retirement criteria, dependency migration, evidence preservation,
  communication, long-term archive, immutable package state, and active
  dependency closure are contract-validated.
- [x] Executed package criteria, migrations, preserved evidence, communications, archive,
  configuration, and version bind exactly to the request before result-digest validation.
- [x] Seven-control preflight fails closed and cannot infer issuer identity,
  consent, treatment, kinase activity, or generic all-omics conclusions.
- [x] API, SDK, CLI, and strict parse-once plugin share one deterministic
  service boundary and preserve canonical request/result identity.
- [x] Unsatisfied criteria, blocked migration, unavailable evidence,
  unacknowledged communication, unverified archive, authorization failure,
  and active dependencies remain visible as safe abstentions.
- [x] Replay rejects altered request/result content, forged IDs, validly
  rehashed result-ID edits, malformed objects, and cross-instance plugin
  tokens.
- [x] CLI refuses unsafe overwrites and exits non-zero on abstention or
  malformed input; API errors are sanitized.
- [x] Frozen evaluator, adversarial tests, branch coverage, benchmark,
  release verifier, package hashes, isolated import, LOC, and traceability
  evidence are captured from the clean worktree.

## External CI note

GitHub Actions runs for the repository may fail before any job step when the
account has failed payments or the spending limit needs provisioning. That is
an external billing/account condition, not a local code-gate failure. The
reproducible local gates and `tools/verify_m2608_release.py` output remain the
authoritative evidence until Actions capacity is restored.
