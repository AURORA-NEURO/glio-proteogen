# M08-02 release evidence

This evidence is local-only and explicitly provisional until Scientific engineering
freezes the public ABI, feature catalogue, estimator, metrics, and media type.

## Authority and boundary

- Dossier SHA-256: `0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`.
- Exact authority slice: lines `2600-2643`.
- Responsibility: representation and feature construction beneath Transcript-protein
  discordance, including scaling, masks, covariates, provenance, and leakage-safe
  transformations.
- Parent target: `protein_subtype`; this module emits no parent claim.
- Owner/safety/gate: Scientific engineering / S2 / G1.
- ABI status: `0.1.0-provisional`; operation, feature vocabulary, limits, and media
  type are implementation metadata pending owner confirmation.

## Gates

- Focused contract/runtime/interface/evaluator suite: **24 passed**, warnings treated
  as errors.
- Ruff: clean across M08-02 contracts, runtime, interfaces, evaluator, benchmark, and
  tests; Ruff format clean.
- MyPy strict: clean across 14 checked source/evaluation files.
- Compile gate: bundled Python 3.12 compileall passed for runtime and evaluator.
- Scoped branch coverage: **95%** across 530 statements and 90 branches for the M08-02
  contract/runtime/interface surface.
- Evaluator: constructed lineage-safe path, complete leakage checks, outcome-field
  abstention, duplicate-source abstention, deterministic replay all pass.
- Benchmark: 10 iterations, mean **2,228,980 ns**, median **2,229,600 ns**, p95
  **2,324,000 ns**, under provisional 2,000,000,000 ns mean / 3,000,000,000 ns p95
  budgets.
- Package: wheel and sdist built with hatchling 1.31.0; wheel size 763,012 bytes and
  sdist size 1,431,847 bytes.
- Isolated import: wheel installed into a clean target and imported
  `glio_proteogen`, M08-02 contracts, and `M0802RepresentationEngine` successfully.

## Safety and negative-path coverage

The suite covers withheld consent, unresolved identity, rejected controls, future /
outcome / target / label / response leakage, duplicate source artifacts, wrong
M08-01 handoff media type, duplicate feature specifications, duplicated handoff,
malformed JSON, duplicate keys and non-finite JSON, unknown schema, non-canonical
replay bytes, digest mismatch, result feature/check closure, plugin token misuse,
CLI invalid requests, and CLI output overwrite.

## Local commits

1. `e057c72` — close representation replay contract.
2. `11cb21d` — deterministic lineage-safe runtime, provenance, uncertainty, and replay.
3. `5691cf4` — strict API, CLI, and parse-once plugin parity.
4. `c525750` — evaluator, fixtures, lifecycle, and adversarial coverage.
5. `759c4b0` — interface export and coverage formatting closure.
6. `2fc7f10` — authority-bound evaluator scenario fixture.

