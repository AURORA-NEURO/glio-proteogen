# M26-07 release evidence

| Field | Evidence |
| --- | --- |
| Module | `GLIO-PROTEOGEN-M26-07` |
| Authority | SHA-256 `0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`; `GLIO-PROTEOGEN_240_Module_Dossier.md:9300-9340` |
| Owner / safety / gate | Platform engineering / S3 / G5 |
| Parent | protein subtype |
| Contract | `0.1.0-provisional`; no frozen ABI or issuer authority is claimed |
| Fixture | eight locked scenarios; digest recorded in `release-evidence/m26_07/evaluation.json` |
| Focused tests | 33 passed across contract, runtime, interface, evaluator, adversarial, and release-verifier suites |
| Python contribution | 2,009 added / 15 deleted / 1,994 net lines from scaffold `5984062f` atop M26-06; breakdown in `release-evidence/m26_07/loc.json` |
| Scoped coverage | branch-enabled report recorded in `release-evidence/m26_07/coverage.json`; 95.15885022692889% combined, fail-under 95 |
| Evaluator | 8/8 scenarios passed: approval, abstention, regression, preflight, parity, tamper, determinism, schema authority |
| Benchmark | 10 calls; mean 1,489,220 ns; p95 2,448,800 ns; budgets 500 ms / 750 ms |
| Package | wheel/sdist hashes and isolated import recorded in `release-evidence/m26_07/package.json` |

## Review checklist

- [x] Required revalidation IDs, proposal references, no-regression comparisons,
  staged approval, and tested rollback are contract-closed.
- [x] Seven-control preflight rejects unauthorized material before change
  traversal.
- [x] API, SDK, CLI, and strict parse-once plugin share the same service and
  canonical replay boundary.
- [x] Failed revalidation and unsafe inputs remain visible while producing a
  safe review-required abstention.
- [x] Seven uncertainty dimensions are explicit `not_estimable` values; no
  biological or clinical estimate is fabricated.
- [x] Replay rejects forged IDs, request digests, payload digests, and altered
  result content.
- [x] CLI refuses to overwrite existing output and exits non-zero on abstention.
- [x] Release verifier, coverage, benchmark, package hashes, and isolated
  imports are captured from a clean checkout.
