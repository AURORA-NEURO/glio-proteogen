# M26-04 release evidence

| Field | Evidence |
| --- | --- |
| Module | `GLIO-PROTEOGEN-M26-04` |
| Authority | SHA-256 `0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`; `GLIO-PROTEOGEN_240_Module_Dossier.md:9168-9208` |
| Owner / gate | Quality engineering / S3 / G2 |
| Parent | protein subtype |
| Contract | `0.1.0-provisional`; no frozen ABI or catalogue is claimed |
| Fixture | eight locked scenarios; digest recorded in `release-evidence/m26_04/evaluation.json` |
| Focused tests | 45 passed (contract, runtime, interfaces, evaluator, adversarial) |
| Scoped coverage | branch-enabled report recorded in `release-evidence/m26_04/coverage.json`; fail-under 95% |
| Evaluator | 8/8 scenarios passed; complete publication, all safe abstentions, replay/tamper, determinism, and schema authority |
| Benchmark | 10 calls; mean and p95 recorded in `release-evidence/m26_04/benchmark.json`; budgets 500 ms mean / 750 ms p95 |
| Package | wheel/sdist hashes and isolated import recorded in `release-evidence/m26_04/package.json` |

## Review checklist

- [x] Contract fields and cross-references are closed and duplicate-free.
- [x] Declared mass-spectrometry, genome/transcriptome, and PTM modality
  artifacts are bound exactly (ID, version, digest, and media type) into the
  source-artifact set; rebound or duplicate modality identities are rejected.
- [x] Published access surfaces bind exact request operations, media types,
  authorizations, idempotency records, jobs, compatibility, errors, audit and
  configuration material; rehashed semantic mutations are rejected.
- [x] Seven-control preflight rejects unauthorized material before publication.
- [x] API, SDK, CLI, and plugin parse and replay the same canonical envelope.
- [x] Safe abstention preserves findings without exposing an access surface.
- [x] Seven uncertainty dimensions are explicit `not_estimable` values; no
  biological or clinical estimate is fabricated.
- [x] Replay rejects forged IDs, request digests, payload digests, and altered
  result content.
- [x] HTTP request/replay streams and direct service mappings enforce the distinct
  8 MiB request and 16 MiB result ceilings before strict validation.
- [x] CLI refuses to overwrite an existing output and exits non-zero on
  abstention.
- [x] Package evidence is generated from a clean checkout and includes only
  the module's declared runtime surface.
