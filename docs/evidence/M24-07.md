# M24-07 release evidence

| Item | Result |
| --- | --- |
| Authority | SHA-256 `0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`, `GLIO-PROTEOGEN_240_Module_Dossier.md:8580-8620` |
| Dependency | M24-06 exact upstream artifact identity (ID/version/digest/media); no unpublished runtime ABI imported |
| Focused tests | 32 passed across contract, runtime, interface, evaluator and adversarial suites |
| Scoped coverage | 100.0% branch-enabled total; 539 statements, 88 branches, 0 misses/partials |
| Evaluator | 6/6 locked scenarios plus 11/11 adversarial cases; supported, metric/fallback abstention, denied control, deterministic replay, and self-rehashed semantic replay rejection |
| Benchmark | 10 iterations; mean 3,464,850 ns; median 3,400,400 ns; p95 4,488,900 ns |
| Budgets | 500,000,000 ns mean; 750,000,000 ns p95; passed |
| Static/import | Ruff check/format, strict MyPy, compileall, isolated wheel import, independent release verifier |
| Package | Hashes and sizes are frozen in `release-evidence/m24_07/package.json`; generated outputs are excluded from source |

M24-07 emits human-factors and operational validation material only. It does not
authenticate source truth, emit a biomarker-panel conclusion, infer biology,
provide treatment or kinase claims, or infer identity or consent.

The request source manifest retains the exact declared M24-06 upstream artifact;
omission and same-ID substitutions with altered version, digest, or media type
are rejected before evaluation.

Result evidence retains metric, fallback, configuration and seven-control evidence
references, and provenance input digests bind every emitted evidence artifact.

The locked evaluator includes the same semantic replay threat model as the
adversarial suite: a nested report and evidence mutation is rehashed and must
still be rejected by regeneration from the embedded request.
