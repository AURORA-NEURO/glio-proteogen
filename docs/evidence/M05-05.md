# M05-05 release evidence

## Scope

The branch is based on M04-05 merge `d20108e1` and preserves the existing
shared API, CLI, plugin, and verifier routes. Python-only delta from that base
at the evidence checkpoint is 3,963 added lines across 17 files (including
tests and evaluation harnesses). The contract remains provisional.

## Gates

| Gate | Result | Evidence |
| --- | --- | --- |
| Focused contract/runtime/interface/hardening suite | 54 passed | `tests/contract/test_m05_05_contracts.py`, lifecycle, hardening, integration |
| Evaluator | 8/8 passed | `release-evidence/m05_05/evaluation.json` |
| Benchmark | passed | 25 iterations; mean 1,785,038,752 ns; p95 2,313,523,700 ns; maximum 2,372,226,400 ns; budgets 2,000,000,000/3,000,000,000 ns |
| Ruff/MyPy | pending final release rerun | targeted source/eval/test checks were clean before evidence packaging |
| Scoped branch coverage | not release-green | 962/1,027 statements, 223/280 branches, 90.66564651874522%; fail-under 95 |
| Package | pending | build is intentionally not represented as passed until coverage closure and clean artifact audit complete |

The coverage gap is recorded explicitly. This evidence package must not be
treated as merge-ready until the missing validator/engine branches are closed
with substantive tests and the isolated scoped report reaches the repository
threshold.

## Safety and replay

The evaluator includes clear, seeded-critical, contamination-detected,
missing, unsupported, ledger-binding-only, quarantined-upstream, and
abstained-upstream scenarios. Hardening tests cover unknown-field rejection,
duplicate-key rejection, hostile mapping access, byte limits, nested digest
tampering, replay-context mismatch, token forgery, and safe abstention. No
scenario permits identity, consent, treatment, kinase, protein, proteoform,
isoform, glioma-specific, or all-omics inference.
