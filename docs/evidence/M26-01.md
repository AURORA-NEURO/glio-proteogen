# GLIO-PROTEOGEN M26-01 evidence

M26-01 is a provisional immutable registry and active-configuration resolver
beneath the Proteomics standards registry, with parent target `protein
subtype`. It emits only a typed registry/configuration registration result;
it never infers subtype claims, kinase activity, treatment recommendations,
identity, consent, or generic all-omics outputs.

Authority is the permitted dossier SHA-256
`sha256:0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`,
exact slice `GLIO-PROTEOGEN_240_Module_Dossier.md:9036-9076`. The ABI remains
`0.1.0-provisional`, owner Computational biology, safety class S3, and gate
G0. Caller-declared registry material is opaque evidence; no unpublished
sibling service or upstream runtime dependency is invented.

The deterministic engine requires seven-control authorization, complete
typed registry entries, append-only history coverage, all seven active
configuration kinds, exact compatibility digests, source-artifact binding,
evidence, provenance, limitations, and seven typed uncertainty dimensions.
Quarantined, superseded, revoked, incompatible, malformed, or unauthorized
inputs fail closed to a review-required abstention without registry or
configuration projections. Canonical request/result digests and deterministic
result identifiers close replay and tamper verification.

The strict FastAPI, Typer, service, and parse-once plugin seams preserve the
same canonical result, sanitize errors, reject duplicate JSON keys, and refuse
output overwrite. Frozen evaluator and release records are under
`release-evidence/m26_01/` and are checked by `tools/verify_m2601_release.py`.
