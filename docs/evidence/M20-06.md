# M20-06 release evidence

| Item | Result |
| --- | --- |
| Authority | SHA-256 `0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`, `GLIO-PROTEOGEN_240_Module_Dossier.md:7096-7136` |
| Dependency | M20-05 finalized at `d14cdca0`; exact ancestor edge verified; M20-04 at `c59e5217` |
| Contract | Blinded assignments, discrepancy preservation, immutable audit history, resolved/escalated closure, replay and abstention |
| Focused tests | 30 passed |
| Scoped coverage | 96% branch-enabled; 326 statements, 317 covered; 56 branches, 48 covered, 8 partial; fail-under 95% |
| Evaluator | 9/9 declared cases; one nominal resolution plus 8/8 adversarial scenarios passed |
| Benchmark | 25 iterations; mean 1,622,484 ns; median 1,515,800 ns; p95 2,535,400 ns |
| Budgets | 500,000,000 ns mean; 750,000,000 ns p95; passed |
| Static/import | Ruff check/format, strict MyPy across targeted M20-06 source/eval files, compileall, isolated import and release verifier |

This module emits a structured adjudication record for human review only. It
does not emit a protein-subtype conclusion (`emits_parent=false`) and does not
authenticate, reinterpret, or traverse the caller-declared M20-05 artifact.
Package hashes, evaluator/benchmark digests, and the exact provisional ABI are
recorded under `release-evidence/m20_06/`. Generated coverage, build, and
installation directories are excluded from the source tree.

The request source manifest binds the declared M20-05 workflow result by the
complete artifact identity tuple `(artifact_id, version, digest, media_type)`.
Version, digest, and media-type substitutions are rejected even when the
remaining identity fields match; this closes the upstream source boundary
without changing the provisional ABI.
