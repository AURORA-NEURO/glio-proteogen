# M06-02 evidence record (provisional)

This record is local-only evidence for the dossier slice at lines 1880–1920.

- Dossier SHA-256: `0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`
- Contract hardening commit: `6ddd72d`
- Runtime/replay commit: `e8d2a92`
- API/CLI/plugin commit: `25e5fde`
- Status: provisional; not pushed; no release authority claimed.

## Gate results

- Ruff: clean for M06-02 contracts, runtime, interfaces, evaluator and benchmark.
- MyPy: clean for 14 checked source/evaluator files.
- Focused contract, lifecycle, plugin, API, CLI and evaluator tests: 36 passed.
- Branch-enabled coverage for the M06-02 contract/runtime source set: 99% (519
  statements; `coverage report --fail-under=95`).
- Evaluation: `evals/m06_02/run.py` verifies deterministic construction, explicit
  unsupported-feature masks, abstention and replay verification.
- Benchmark: `evals/m06_02/benchmark.py` measures the public construct boundary and
  records mean `1284480 ns` and p95 `1510500 ns` over 10 iterations against
  provisional mean/p95 budgets of `2000000000/3000000000 ns`.
- Package: wheel/sdist and installed-wheel import checks are local release gates.

## Safety observations

The constructor preserves caller-declared values and never treats missing or
unsupported features as negative findings. Consent, identity and accepted-control
gates run before construction. API and CLI ingress reject duplicate JSON object keys,
bound request size and sanitize validation failures. Replay requires canonical bytes
and a matching result digest.
