# M05-08 evidence record (provisional)

This record is local-only evidence for the dossier slice at lines 1784–1824.

- Dossier SHA-256: `0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`
- Contract hardening commit: `1ad52e2`
- Runtime/lifecycle commit: `bb42e50`
- API/CLI integration commit: `221091e`
- Status: provisional; not pushed; no release signing authority claimed.

## Gate results

- Ruff: clean for M05-08 contracts, runtime, evaluation, benchmark and focused tests.
- MyPy: clean for M05-08 contracts and runtime packages.
- Focused contract, lifecycle, API, CLI and evaluator tests: 57 passed.
- Branch-enabled coverage for the M05-08 contract/runtime source set: 95% (716
  statements; `coverage report --fail-under=95`).
- Evaluation: `evals/m05_08/run.py` verifies default quarantine, injected-verifier release,
  package replay and safe failure.
- Benchmark: `evals/m05_08/benchmark.py` measures only the public build boundary and
  records mean `1092270 ns` and p95 `1704600 ns` over 10 iterations against provisional
  mean/p95 budgets of `2000000000/3000000000 ns`.
- Package: Hatchling built wheel and sdist successfully; source import check passed.
  See `M05-08-package.json` for artifact sizes and the exact local gate record.

## Safety observations

The default service has no verifier and never emits package bytes. Artifact paths,
byte types, declared sizes and SHA-256 references are checked before assembly. Package
inspection rejects digest, manifest, signature, policy and member-set tampering. API
and CLI ingress reject duplicate JSON object keys and use sanitized validation output.
