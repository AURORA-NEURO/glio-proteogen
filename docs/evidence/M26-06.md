# M26-06 release evidence

| Field | Evidence |
| --- | --- |
| Module | `GLIO-PROTEOGEN-M26-06` |
| Authority | SHA-256 `0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`; `source-manifest.yaml:9256-9296` |
| Owner / gate | Data engineering / S3 / G4 |
| Parent / input | protein subtype / provisional M26-05 standards result |
| Contract | `0.1.0-provisional`; no frozen ABI or catalogue is claimed |
| Fixture | six locked security and privacy scenarios; digest recorded in `release-evidence/m26_06/evaluation.json` |
| Focused tests | 33 passed across contract, runtime, interfaces, evaluator, and adversarial suites |
| Scoped coverage | 98.29% combined and 96.25% branch-enabled; fail-under 95% |
| Evaluator | 6/6 scenarios passed; allow, control-failure abstention, unresolved abstention, fail-closed consent, replay/tamper, and determinism |
| Benchmark | 10 calls; mean and p95 recorded in `release-evidence/m26_06/benchmark.json`; budgets 500 ms mean / 750 ms p95 |
| Package | wheel/sdist hashes and isolated import recorded in `release-evidence/m26_06/package.json` |

## Review checklist

- [x] Every request binds all eight caller-declared security controls to a
  typed status, rationale, evidence reference, upstream M26-05 media type,
  and request digest.
- [x] Seven-control preflight rejects unauthorized, revoked-consent, missing
  provenance, unsupported, and wrong-intended-use requests before protected
  payload traversal.
- [x] API, CLI, SDK, and strict JSON plugin parse bounded input once and share
  canonical service behavior; plugin execution requires an opaque seal.
- [x] Failed, unresolved, or unsupported controls produce explicit safe
  abstention and review posture; the runtime never infers consent, identity,
  treatment, kinase activity, or biological negative evidence.
- [x] Results carry control findings, audit evidence, provenance, seven
  `not_estimable` uncertainty dimensions, and canonical request/result
  digests with replay and tamper verification.
- [x] CLI refuses to overwrite an existing output and exits 3 on abstention;
  errors are sanitized at the API and CLI boundaries.
- [x] Release verifier checks authority, fixture, evaluator, coverage,
  benchmark, and package evidence before publication.
