# M10-03 evidence receipt

The deep-build includes contract/replay closure, deterministic runtime/service/plugin, FastAPI/Typer parity, evaluator fixtures, adversarial tests, and a reproducible benchmark wrapper. The fixture-bound evaluator executes 11 cases covering supported interval, scalar, and categorical shapes, deterministic replay, tamper rejection, rejected upstream controls, parent non-emission, locked preprocessing, and locked tuning. Numeric estimates and diagnostic metrics reject non-finite values before serialization.

Replay verification now regenerates the complete result from the embedded request and compares the canonical result envelope. It also validates the request binding during strict reparse, so recomputing a nested payload's `result_digest` cannot turn a forged request, estimate, diagnostic, or provenance field into a valid receipt. The regression matrix in `tests/modules/test_m10_03_baseline_runtime.py` exercises self-rehashed mutations across each of those fields.

The public ABI remains provisional pending owner confirmation. No scientific-content fetch, model download, network traversal, kinase ownership, all-omics fusion, treatment recommendation, identity inference, or consent inference is performed.

Release receipt: `release-evidence/M10-03/` records the 11-case evaluation, 10-iteration benchmark, 96.0% scoped branch coverage, and wheel/sdist hashes. `tools/verify_m1003_release.py` validates fixture binding, budget closure, coverage threshold, archive member counts, and isolated import evidence.
