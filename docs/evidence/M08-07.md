# M08-07 calibration and selective prediction evidence

This release package implements the dossier slice for **GLIO-PROTEOGEN-M08-07 — calibration
and selective prediction** (authoritative dossier SHA-256
`0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`, lines 2820-2863).
The module is explicitly provisional because the dossier does not freeze an endpoint, media
type, calibration catalogue, or upstream M08-06 ABI. No ABI is presented as owner-approved.

| Evidence | Artifact | Verification |
| --- | --- | --- |
| Contract closure | `src/glio_proteogen/contracts/m08_07/` | Strict frozen request, candidate, configuration, estimate, prediction-set, diagnostic and result models; duplicate references/IDs, finite bounded values, exact request/result digests, and no unsupported-to-negative path |
| Runtime | `.../m08_07_calibration_selective_prediction/engine.py` | Deterministic scope, support, OOD, nominal coverage, calibration-error and subgroup-disparity gates; supported candidates emit only calibrated estimate/prediction set/support decision; all other paths abstain safely |
| Replay | `contracts/m08_07/canonical.py`, service verify | Canonical request binding and result digest recomputation; tampered status, request, and digest payloads are rejected |
| Interfaces | `api.py`, `cli.py`, `plugin.py` | Strict duplicate-free JSON, sanitized validation errors, schema export, no-overwrite CLI behavior, opaque validate-then-run capability, and API/CLI/library parity |
| Evaluator | `evals/m08_07/run.py` | Supported candidate, missing candidate review, support/OOD abstention, coverage review, replay, tamper rejection, and plugin parity |
| Benchmark | `evals/m08_07/benchmark.py` | Ten deterministic public service calls after warm-up; provisional 2e9 ns mean / 3e9 ns p95 budgets |
| Focused tests | `tests/contract/test_m08_07_*`, `tests/modules/test_m08_07_*`, `tests/integration/test_m08_07_interfaces.py` | Contract adversaries, runtime gates, replay/tamper, API, CLI, plugin, evaluator, benchmark |
| Traceability | `docs/traceability/GLIO-PROTEOGEN-M08-07.csv` | Responsibility, boundary, quality, uncertainty, review, safety, test, benchmark and recovery requirements mapped to executable evidence |
| Manifest | `docs/modules/M08-07.manifest.md` | Data/model/authority claims ceiling and rollback boundary |

## Safety and claims ceiling

- The only emitted parent target is the typed `protein_subtype` object; `emits_parent` is false.
- The operation emits no kinase activity, generic all-omics fusion, treatment recommendation,
  raw spectra, peptide/accession/sequence content, identity inference, consent inference, or
  upstream mutation.
- Missing candidates, unsupported scopes, low support, OOD candidates, coverage outside 85%-95%,
  calibration error above its configured ceiling, and subgroup disparity above its configured
  ceiling cannot become a negative finding. They return a typed abstention with explicit support
  status and review state.
- Candidate metrics are caller-declared and not issuer-authenticated. A calibrated status is only
  the deterministic result of the declared gates; this package proves contract behavior, not model
  validity, calibration validity, transportability, biological truth, or clinical utility.
- Seven control references are checked before candidate traversal. Consent and identity are never
  inferred locally.

## Recovery

Results are immutable and content-addressed. Corrections must be submitted as a new request with
`supersedes_result_digest`; no prior request/result/configuration is overwritten, relabeled, or
silently promoted. Critical discrepancy, OOD state, support override, claim promotion, release
exception, and unresolved biological conflict remain subject to external human review.

