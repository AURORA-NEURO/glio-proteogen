# M16-07 release evidence

## Authority

The implementation is traceable to dossier SHA-256
`0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`, exact lines `5700-5740`.
The implementation metadata and endpoint names remain provisional because the dossier does not
freeze the final ABI, endpoint catalogue, or media registry.

## Granular implementation chain

| Stage | Commit | Evidence |
| --- | --- | --- |
| contract/replay hardening | `6b587f2` | signed consumer/ownership closure, canonical request/result digests, seven-dimension uncertainty/provenance |
| deterministic runtime | `5f0ffde` | seven-control preflight, compatibility gates, safe abstention, replay/tamper verification, service seam |
| API/CLI/plugin interfaces | `5728923` | strict JSON parse-once adapters, sanitized FastAPI errors, Typer no-overwrite export, sealed plugin token |
| evaluator/benchmark | `c7d1dd2` | ten frozen scenarios and benchmark wrapper |
| adversarial/release closure | `4b75e6f` | contract adversarial tests, release verifier, locked JSON evidence |
| documentation/traceability | pending | this module manifest, evidence record, requirement matrix |
| package evidence | pending | wheel/sdist hashes and isolated import proof |

## Evaluation

`release-evidence/m16_07/evaluation.json` is the locked ten-case report. All 10 declared cases
executed and passed. Fixture digest:
`sha256:6d81a9703ec6b2cce56fa83d57465ea99fda2613b9155e7785e4eae251178ff3`.

The matrix proves:

- supported fields produce a compatible signed export;
- review, ownership, unsupported, OOD, and prohibited markers abstain safely;
- seven controls are enforced before typed request construction;
- repeated construction is byte-equivalent;
- all seven uncertainty dimensions and seven control decisions are present; and
- replay succeeds while a tampered result digest is rejected.

## Performance and coverage

The ten-iteration benchmark report records mean `1,933,740 ns`, median `1,844,200 ns`, and
p95 `2,540,900 ns`, against provisional budgets of `2,000,000,000 ns` mean and
`3,000,000,000 ns` p95.

The scoped branch-enabled coverage report records 546 statements, 536 covered statements,
100 branches, 91 covered branches, combined coverage `97.05882352941177%`, and fail-under `95.0%`.
Scope is the M16-07 contract, downstream runtime, plugin/service, and M16-07 adapter modules;
the evaluator tests execute as the scenario gate but evaluator wrapper internals are not counted
as product source coverage.

## Reproducibility commands

```text
uv run ruff check src/glio_proteogen/contracts/m16_07 src/glio_proteogen/modules/c16_kinophos_object_consumer/m16_07_downstream_typed_export src/glio_proteogen/adapters/m1607.py evals/m16_07 tools/verify_m1607_release.py
uv run mypy --strict src/glio_proteogen/contracts/m16_07 src/glio_proteogen/modules/c16_kinophos_object_consumer/m16_07_downstream_typed_export src/glio_proteogen/adapters/m1607.py evals/m16_07 tools/verify_m1607_release.py
uv run pytest -o addopts='' --cov-reset --cov=glio_proteogen.contracts.m16_07 --cov=glio_proteogen.modules.c16_kinophos_object_consumer --cov=glio_proteogen.adapters.m1607 --cov-branch --cov-report=json:coverage-m1607.json --cov-fail-under=95 tests/contract/test_m16_07_contract.py tests/contract/test_m16_07_deep.py tests/modules/c16_kinophos_object_consumer/test_m16_07_engine.py tests/integration/test_m1607_interfaces.py tests/evals/test_m16_07.py -q
uv run python tools/verify_m1607_release.py release-evidence/m16_07 release-build-m1607-20260815
```

## Safety and review disposition

The provisional release is eligible for human review, not automatic clinical promotion. Any
critical discrepancy, OOD/support override, claim promotion, release exception, or unresolved
conflict remains explicitly review-gated. No endpoint or catalogue should be treated as frozen
until the authority slice is superseded by a signed ABI decision.
