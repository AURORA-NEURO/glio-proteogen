# M11-02 release evidence

- Dossier SHA-256: `0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`
- Authority slice: lines `3680–3720`
- Branch: `module/M11-02-context-subtype-stratifier-provisional`
- ABI: `0.1.0-provisional`; owner confirmation and catalogue freeze remain pending.

## Granular commits

1. `6ddabf9` — harden context-stratifier contract and replay closure
2. `fdd9fb2` — add deterministic context stratifier runtime and replay service
3. `c8e4e2c` — add strict API CLI and parse-once parity
4. `ad3d03f` — add evaluator matrix and adversarial coverage
5. `4a7732d` — add traceability, release evidence, and package receipt
6. `5d86905` — record isolated package verification receipt
7. `1f8c962` — tighten final runtime coverage path

## Gate receipt

The scoped stage command was:

```text
uv run pytest -o addopts='' --cov=glio_proteogen.contracts.m11_02 \
  --cov=glio_proteogen.modules.c11_protein_native_subtype.m11_02_context_subtype_stratifier \
  --cov=glio_proteogen.adapters.m1102 --cov-branch --cov-report=term-missing \
  tests/contract/test_m11_02_contract.py \
  tests/modules/c11_protein_native_subtype/test_m11_02_runtime.py \
  tests/integration/test_m11_02_interfaces.py
```

Result: `24 passed`; 99% scoped branch coverage (`529` statements, `78` branches).
Ruff check/format and strict MyPy are clean for all M11-02 source, adapter, evaluator,
and release-verifier files. Compileall is clean.

The evaluator reports 8/8 checks green. It includes the supported path, low-support and
missing-context abstentions, prohibited-proxy blocking, exact replay, tamper rejection,
denied-consent fail-closed behavior, and repeat determinism. Ten benchmark calls report
mean `1,897,050 ns`, median `1,732,500 ns`, p95 `1,815,500 ns` against provisional mean
and p95 budgets of `2,000,000,000 ns` and `3,000,000,000 ns`.

Package evidence records the final-tip wheel (`3913b2f`) at `790377` bytes with SHA-256
`5c64909253ce6971de54d063788f8c0f5ed63836191b89140a13d7a16ccf8191` and sdist
`1448783` bytes with SHA-256
`34f5114d3d46f6598e36325bc1b3458eebeeafe629b58c5deff17a4e8135f966`. The wheel was
installed to a temporary target and imported successfully; eight M11-02 schemas and the
FastAPI factory were available.

## Safety boundary

The evidence is synthetic and deterministic. It is not a clinical, prevalence, calibration,
kinase, treatment, identity, or generic all-omics claim. Unsupported, missing, proxied, and
low-support context remains explicit and requires review rather than being converted into a
negative biological finding.
