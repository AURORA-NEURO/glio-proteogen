# M19-02 release evidence

## Authority

This lane is traceable to dossier SHA-256
`0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`, exact lines
`6560-6600`. The ABI is `0.1.0-provisional` pending ML engineering owner confirmation.
M19-02 owns cross-source alignment and reconciliation beneath Immunopeptidomic evidence,
supports `proteotype`, and does not own kinase state, generic all-omics fusion, direct
treatment recommendation, identity or consent inference, or unsupported negative findings.

## Granular implementation chain

| Stage | Commit | Evidence |
| --- | --- | --- |
| contract/replay closure | `d5baa28` | seven-dimension closure, source/evidence identity, discrepancy binding, result identity and tamper invariants |
| deterministic runtime/service/plugin | `84db34c` | seven-control preflight, deterministic alignment, typed conflict findings, safe abstention, provenance and replay |
| API/CLI/plugin interfaces | `46694fe` | strict parse-once JSON, FastAPI/Typer parity, sanitized errors, no-overwrite behavior |
| evaluator/benchmark/fixtures | `37c4e02` | eight frozen scenarios, eight adversarial oracles, bounded ten-iteration benchmark |
| adversarial coverage | `1d57d5e` | negative-path replay/status/source closure and scoped branch-enabled coverage |
| documentation/traceability | this evidence record | authority record, module manifest, requirement matrix, release verifier |
| package/release evidence | final package-evidence commit in this branch | wheel/sdist hashes, isolated import, evaluation/benchmark/coverage verification |

## Gate record

Ruff and MyPy strict are clean for the M19-02 source/evaluator/test set; compileall and
isolated-wheel import pass. The evaluator passes 8/8 scenarios and 8/8 adversarial cases.
The 10-call benchmark is recorded in `docs/evidence/m19_02/benchmark.json` against
provisional 500,000,000 ns and 750,000,000 ns budgets. Scoped branch-enabled coverage is
recorded in `docs/evidence/m19_02/coverage.json` and meets the 95% fail-under gate.
Package hashes and exact member counts are recorded in `docs/evidence/m19_02/package.json`.

## Reproducible measurements

The locked ten-iteration benchmark measured a mean of `2,924,470 ns`, p50 of
`2,906,000 ns`, and p95 of `3,348,500 ns`, against provisional budgets of
`500,000,000 ns` mean and `750,000,000 ns` p95. The scoped branch-enabled run covered
`431` statements (`427` covered) and `88` branches (`84` covered), with `98.4585741811175%`
reported coverage and a `95.0%` fail-under threshold.

The release wheel is `927,647` bytes with `403` members and SHA-256
`a3d349504405ed27e269a34b085235a3fd53340633044ffba0f13409ee631153`. The source
distribution is `1,507,306` bytes with SHA-256
`f587c19ce621a6e21b560b85753a1ff938e6a277e2147ea973b142d929f0a819`. Both artifacts
passed isolated import and the standard-library release verifier.

This is an engineering-provisional lane eligible for human review, not automatic clinical
promotion. Upstream evidence is referenced rather than traversed or mutated; unsupported,
missing, and conflicting dimensions remain typed, explicit, and auditable.
