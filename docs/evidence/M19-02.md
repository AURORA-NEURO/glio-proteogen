# M19-02 release evidence

## Authority

This lane is traceable to dossier SHA-256
`0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`, exact lines
`6560-6600`. The ABI is `0.1.0-provisional` pending ML engineering owner confirmation.
M19-02 owns cross-source alignment and reconciliation beneath Immunopeptidomic evidence,
supports `proteotype`, and does not own kinase state, generic all-omics fusion, direct
treatment recommendation, identity or consent inference, or unsupported negative findings.

## Granular implementation chain

| Stage | Commit | Evidence |
| --- | --- | --- |
| merged M19-01 base | `44d25c34` | exact merge of M19-01 final release base `8480abb` / head `9fe61232` |
| provisional contract scaffold | `225617d3` | request/output schema and seven alignment dimensions |
| contract/replay closure | `d5baa28` | seven-dimension closure, source/evidence identity, discrepancy binding, result identity and tamper invariants |
| deterministic runtime/service/plugin | `84db34c` | seven-control preflight, deterministic alignment, typed conflict findings, safe abstention, provenance and replay |
| API/CLI/plugin interfaces | `46694fe` | strict parse-once JSON, FastAPI/Typer parity, sanitized errors, no-overwrite behavior |
| evaluator/benchmark/fixtures | `37c4e02` | eight frozen scenarios, eight adversarial oracles, bounded ten-iteration benchmark |
| adversarial coverage | `1d57d5e` | negative-path replay/status/source closure and scoped branch-enabled coverage |
| documentation/release verifier | `5cc1a179` | authority record, module manifest, requirement matrix, release verifier |
| initial evidence record | `d02811cc` | evaluator, coverage, and package evidence scaffold |
| refreshed evaluation/package evidence | `adf1358f` | current evaluation, benchmark, branch coverage, wheel/sdist hashes, and isolated import |

## Gate record

Ruff check and format are clean for all 19 changed Python files. MyPy `--strict` is clean
for 13 M19-02 implementation/evaluator/tool files; compileall is clean. The focused suite
passes 24/24 tests. The evaluator passes 8/8 scenarios and 8/8 adversarial cases. The
10-call benchmark is recorded in `docs/evidence/m19_02/benchmark.json` against provisional
500,000,000 ns and 750,000,000 ns budgets. Scoped branch-enabled coverage is recorded in
`docs/evidence/m19_02/coverage.json` and meets the 95% fail-under gate. The release verifier
and isolated-wheel import pass; package hashes and exact member counts are recorded in
`docs/evidence/m19_02/package.json`.

Relative to the exact M19-01 merge base `8480abb`, this lane contributes 2,774 added Python
lines and removes one existing test-package line: 1,813 implementation/evaluator/tool
lines plus 961 test lines. The contribution spans 19 Python files and is intentionally
granular across contract, runtime, interface, evaluation, adversarial tests, and release
verification.

## Reproducible measurements

The locked ten-iteration benchmark measured a mean of `1,933,270 ns`, p50 of
`1,888,000 ns`, and p95 of `2,211,800 ns`, against provisional budgets of
`500,000,000 ns` mean and `750,000,000 ns` p95. The scoped branch-enabled run covered
`534` statements (`530` covered) and `96` branches (`92` covered), with
`98.73015873015873%` reported coverage and a `95.0%` fail-under threshold.

The release wheel is `2,396,370` bytes with `1,253` members and SHA-256
`20c3ed6a46326212de4f4c090983848ad8013de074ded7d52bb0db5c1eaca918`. The source
distribution is `2,913,090` bytes with SHA-256
`d1c979f945fea61a7d58326225067bc97695070c9229f6f4a3ca422cbd737d19`. Both artifacts
passed isolated import and the standard-library release verifier.

This is an engineering-provisional lane eligible for human review, not automatic clinical
promotion. Upstream evidence is referenced rather than traversed or mutated; unsupported,
missing, and conflicting dimensions remain typed, explicit, and auditable.
