# M05-02 evidence inventory

| Evidence | Path / command | Locked assertion |
|---|---|---|
| Dossier source | authoritative attachment SHA-256 `0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`, lines 1520-1560 | exact responsibility, boundaries, inputs/outputs, controls, uncertainty, review, acceptance, architecture choices, owner/safety/gate, and future gates |
| Public contracts | `src/glio_proteogen/contracts/m05_02` | strict immutable v1 request/result/policy/DAG plus complete replay helpers |
| Runtime | `src/glio_proteogen/modules/c05_ptm_localization/m05_02_identity_lineage` | authorization-first sealed deterministic reconciliation |
| Genuine builder | `evals/m05_02/run.py::build_scenario_request` | executes genuine M01-02 and M05-01 public operations; creates no handwritten upstream result |
| Locked corpus | `tests/fixtures/m05_02/scenarios.json` | exactly 70 unique cases in 8 named groups |
| Executable eval | `python -m evals.m05_02.run` | declared=executed=passed=70, zero missing/duplicate cases |
| Benchmark | `python -m evals.m05_02.benchmark` | one five-role warm-up + exactly 25 public calls, mean <=400 ms and p95 <=750 ms |
| Focused tests | M05-02 contract/lifecycle/interface/tooling tests | strict schema, firewall, replay, swap/collision/duplication, privacy, recovery, and parity |
| Traceability | `docs/traceability/GLIO-PROTEOGEN-M05-02.csv` | dossier and locked-risk requirements mapped to implementation and evidence |
| Data/model/reference manifest | `docs/modules/M05-02.manifest.md` | synthetic metadata, zero installed models, no storage/network, opaque references |
| Release verifier | `tools/verify_release_artifacts.py m05-02-evidence` | exact corpus, workload shape, iteration/warm-up counts, digests, and timing budgets |
| Current-stack package evidence | `docs/evidence/M05-02-package.json` | base/head, Python LOC delta, clean wheel/sdist hashes, isolated import, static/evaluation/benchmark gate tuple |

The final evidence/package commit is `bc5323d3`, which records the machine-readable package tuple
and the current-base gate record below.

## Acceptance evidence

The contract exposes exactly nine JSON Schema 2020-12 documents. Every supported request contains
all seven physical identity kinds, exactly five artifact roles, one approved derivation, and exact
M01-02/M05-01 bindings. Strict JSON rejects duplicate keys, coercion, unknown members, non-finite
values, invalid media aliases, and request bodies over 4 MiB.

Every valid result embeds the exact request and deterministically rederives all identifiers,
digests, graph nodes/edges, findings, disposition, receipt, support, uncertainty, provenance,
evidence, limitations, review state, and time. Re-signed finding, receipt, graph, provenance, and
result substitutions reject. Semantic-set reordering preserves exact request/result equality.

The corpus executes a genuine two-patient M01-02 lineage and proves cross-patient propagation is
quarantined. Separate cases cover identity swaps; artifact-lineage, artifact-identity, and
binding-scope collisions; duplicate-content retention; five producer drift dimensions; and all
four non-observed evidence states. Every control denial occurs before artifact traversal.

## G0 and future-gate interpretation

- G0: all 70 locked critical cases behave as declared with no silent coercion or merge.
- G1: no scientific primary-error claim is made; the installed checks are hard metadata/lineage
  invariants only.
- G3 robustness: canonical semantic reorder is invariant; unsupported configuration and
  non-evaluable evidence abstain.
- G3 uncertainty: no probability is emitted. Seven dimensions are non-estimable and support is
  explicitly narrowed to exact reviewed bindings, satisfying the dossier alternative to 85%-95%
  nominal coverage.
- G4: all ownership flags are false; provenance and receipts are exact; recovery is append-only.

## Reproduction

```text
uv run python -m evals.m05_02.run --output m05-02-eval.json
uv run python -m evals.m05_02.benchmark --output m05-02-benchmark.json
uv run python tools/verify_release_artifacts.py m05-02-evidence m05-02-eval.json m05-02-benchmark.json
```

These artifacts do not constitute reviewer approval, biological validation, calibration, or
scientific/clinical qualification.

## Current-stack gate record

This lane was transplanted additively onto merged base `e783beb8b8c27258d2371029eeaf123d3233b580`
(the merged M05-01 stack, whose parent is the M04-06 merge `61369eca`). The implementation sequence
is `7abf81e9` (contract and replay spine), `c4cfc3f2` (runtime, service, plugin, evaluator entry
points, and interfaces), `68bddfc7` (70-case fixture, benchmark, adversarial hardening, release
workflow, and verifier), `3953b83a` (additive API/CLI import closure), and `79a34f12` (merged
interface/verifier formatting and strict-dispatch closure), followed by `bc5323d3` (current-stack
metrics and package evidence). The stack preserves all inherited
M04-06/M05-01 API, CLI, CI, and release-verifier routes. The current diff is 19 Python files,
6,095 additions, 2 deletions, net **6,093 Python LOC**.

The focused M05-02 contract/lifecycle/hardening/interface matrix passed **66 tests**. Ruff check
and format passed; strict MyPy passed for **19 targeted source/eval/test/tool files**; py_compile
and compile gates passed. Scoped branch-enabled coverage passed at **98.02110817941953%**: 1,176
statements with 1,161 covered, 340 branches with 325 covered and 15 partial; fail-under was 95.
The generated coverage report was removed after recording these values.

The executable evaluator passed **70/70 cases across 8 groups**. Its canonical report digest is
`1e69eaae4342b27bb0cb017679951d1c5df105c76015d62ed56f6d93dd86d42d`, and the emitted artifact
SHA-256 is `56c16ecb714df9d28d226d0a5a988342799c2cec463739ad0321b06e1c8957c4`. The 25-call
benchmark passed with request/result sizes 53,976/73,657 bytes, mean **346,204,124 ns**, p50
339,184,900 ns, p95 **407,364,100 ns**, and maximum 412,884,900 ns against 400,000,000/750,000,000
ns budgets. The canonical report digest is
`f4908fcb0b24700c3d741c4492f0ebe2d40dd60cbe10c412ba97771f1a7b8341`, and the emitted artifact
SHA-256 is `d14474b252e6a9c7b911b87c2571e1b568876fc04ca1552f0f30386fc7c32907`. Both reports
passed `tools/verify_release_artifacts.py m05-02-evidence` before their temporary files were
removed.

The clean current-base package smoke produced wheel
`glio_proteogen-0.1.0-py3-none-any.whl`, 3,458,317 bytes, SHA-256
`1b5f64c7e3418df44816bc1f0470f489b6b3341a8503b6d8b7f72183003723d8`, with 1,845 members; and
sdist `glio_proteogen-0.1.0.tar.gz`, 3,861,883 bytes, SHA-256
`c8324097a4a07282bccac5f2725d9f11c0945a431a548e9652b6921ae6c655d2`, with 4,224 members. The
exact generated-member audit found zero matches in either archive for coverage reports, temporary
M05-02 build/install paths, coverage state, build/dist directories, pycache, or bytecode. A clean
target wheel install imported all 9 M05-02 schemas and an M04-06 schema; the release verifier also
passed. Machine-readable details are in `docs/evidence/M05-02-package.json`.
