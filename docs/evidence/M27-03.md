# M27-03 release evidence

| Evidence | Exact implementation |
| --- | --- |
| Authority | Dossier SHA `0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`, lines 9484-9524 |
| Contract | `src/glio_proteogen/contracts/m27_03/`; JSON Schema 2020-12 exports for request, output, DAG, node, edge, execution, package, and safe-failure |
| Runtime | `src/glio_proteogen/modules/c27_complex_activity/m27_03_reproducible_pipeline_orchestrator/` |
| Evaluator | `evals/m27_03/run.py`, locked synthetic fixtures in `evals/m27_03/fixtures.py` |
| Benchmark | `benchmarks/m27_03_reproducible_pipeline.py`, ten complete public calls |
| Adversarial tests | cycle/disconnection, duplicate source identity, rejected control, tamper/replay, strict JSON, token, and sanitized API paths in `tests/modules/c27_complex_activity/test_m27_03_orchestrator.py` |
| Safety boundary | No external traversal, raw payload, identity/consent inference, protein/proteoform/isoform inference, kinase, all-omics, treatment, or unsupported-negative conversion |

## Required gates

The focused test command passes 21 tests. Ruff format/check and compileall pass for all changed
M27-03 source, evaluator, benchmark, and test files. The evaluator reports five passing scenarios,
including supported/replay determinism, control abstention, no-package safe failure, and plugin
parity. The locked benchmark records the machine-dependent sample below; it is only a software
regression tripwire.

```json
{"iterations": 10, "mean_ns": 2091910.0, "median_ns": 1897500.0, "p95_ns": 3773500.0}
```

The provisional budgets are 500,000,000 ns mean and 750,000,000 ns p95; this sample is within both.
The scoped branch-enabled coverage gate is 95.19% (656 statements, 134 branches) with fail-under
95, and strict MyPy passes across 11 targeted source/test files.
The package builder is environment-dependent because the dedicated environment does not include
Hatchling; package hashes must be generated by the release environment and must not be fabricated.

## Dependency and authority safety

M27-02 is contract-only on the parent branch at this implementation point. M27-03 therefore
accepts a typed caller-declared M27-02 artifact reference and verifies only its media type. It does
not import, call, or infer an M27-02 runtime endpoint. This keeps the lane honest until the upstream
ABI is frozen by its owner.

## Reproduction

```text
PYTHONPATH=src;.venv/Scripts/python -m pytest --no-cov tests/contract/test_m27_03_provisional.py tests/modules/c27_complex_activity/test_m27_03_orchestrator.py -q
PYTHONPATH=src;. .venv/Scripts/python -m evals.m27_03.run
PYTHONPATH=src;. .venv/Scripts/python benchmarks/m27_03_reproducible_pipeline.py
```

No patient data, raw molecular payload, direct identity, clinical label, or external scientific
content is included in the fixture.
