# GLIO-PROTEOGEN-M03-08 module manifest

| Property | Locked value |
| --- | --- |
| Module | `GLIO-PROTEOGEN-M03-08` |
| Title | Provenance and release packaging |
| Component | C03 Protein inference/ambiguity control |
| Owner | Computational biology |
| Version | `1.0.0` |
| Safety class | `S2` |
| Evidence gate | `G1` |
| Operation | `package_protein_inference_release` |
| Inputs | Authorized exact M03-01..M03-07 outputs, eight declared caller artifact byte streams, pinned release policy and reproducibility declarations, plus an external signature statement and verifier boundary |
| Output ceiling | Deterministic release result, canonical reproducibility manifest, ten-member archive, and exact signature-verification receipt |
| Descriptor assurance | Result reconstruction rederives canonical-USTAR byte size only; archive digest/content equality and authenticity require the public verify API with package bytes |
| Archive inventory | Exactly eight caller members plus generated manifest plus generated verification receipt |
| Maximum metadata shape | Exactly 64 software-version and 64 reference-version records |
| Signature semantics | External statement binds the pre-receipt manifest digest; verification is injected; no signing, key custody, signer identity, certificate, or authority inference |
| Parent target | `complex_activity` (workflow support only; no complex-activity inference) |
| HTTP surface | JSON Schema GET only; deliberately no HTTP build or verify POST and no HTTP binary response |
| Runtime model | Stateless deterministic canonical packaging; no learned model, event store, database, key manager, or external reference lookup |
| Evidence data | Synthetic non-clinical artifacts and genuine public M03-01..M03-07 execution results only |
| Claims ceiling | Deterministic synthetic package/replay regression only; not scientific reproducibility, cryptographic assurance, release authority, supply-chain, assay, biological, or clinical validation |

M03-08 owns only C03 release-chain closure, canonical manifest construction, archive assembly, and
recording one injected external verification outcome. It does not own or mutate upstream
computation, raw parsing, identity or consent inference, scientific interpretation, signature
issuance, complex-activity or proteotype inference, KINOPHOS kinase activity, generic fusion,
treatment recommendation, or clinical decision support. Locked scenarios live under
`tests/fixtures/m03_08`; executable links are in the traceability matrix.
