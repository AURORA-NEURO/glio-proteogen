# GLIO-PROTEOGEN-M14-05 — longitudinal and evolutionary model

## Authority and ownership

- Dossier: `GLIO-PROTEOGEN_240_Module_Dossier.md`
- Dossier SHA-256: `0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`
- Exact slice: lines `4892-4932`
- Responsibility: longitudinal and evolutionary model beneath Microenvironment protein deconvolution
- Owner / safety / gate: Computational biology / S2 / G2
- Parent target: `protein_subtype`
- ABI status: provisional; the implementation below is a deterministic metadata replay boundary only

## Allowed boundary

The runtime accepts strict, caller-declared ordered observations and immutable references. It emits a
time-indexed trajectory of reference-preserving states, explicit not-evaluable change-point records,
typed uncertainty, provenance, diagnostics, support status, evidence, and limitations. It verifies
temporal ordering and declares that future leakage is blocked.

No source bytes are traversed. No biological state, subtype, change point, kinase activity, generic
all-omics fusion, treatment recommendation, identity, consent, or issuer-authenticity claim is made.
Unsupported or missing evidence is not converted into a negative finding. Parent output emission is
explicitly false and human review remains required.

## ABI and interfaces

- Contract: eight strict JSON Schema 2020-12 documents, with authority metadata and explicit
  `provisionalAbi: true`.
- Runtime: `M1405EvolutionEngine` and `M1405Service`; replay verifies request and result digests.
- Plugin: token-sealed validation/run boundary, strict JSON input, descriptor owner `Computational biology`.
- HTTP: `GET /v1/contracts/M14-05/{name}/schema` and
  `POST /v1/modules/M14-05/longitudinal-evolution`.
- CLI: `glio-proteogen longitudinal-evolution export-schema NAME` and
  `glio-proteogen longitudinal-evolution infer REQUEST.json`.

## Evidence requirements

Temporal ordering, absence of future leakage, explicit change-point representation, uncertainty,
safe abstention/validation failure, provenance, package replay, and owner review are covered by the
focused tests, locked evaluator, benchmark, traceability CSV, and package verifier recorded in
`docs/evidence/M14-05.md`.
