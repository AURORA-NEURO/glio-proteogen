# GLIO-PROTEOGEN-M08-07 module manifest

| Property | Provisional declaration |
| --- | --- |
| Module | GLIO-PROTEOGEN-M08-07 |
| Responsibility | Calibration and selective prediction beneath Transcript-protein discordance |
| Parent target | `protein_subtype` |
| Owner / safety / gate | Clinical science / S2 / G3 |
| Version | 0.1.0-provisional |
| Authority | Dossier SHA `0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`, lines 2820-2863 |
| Inputs | Caller-declared M08-06 uncertainty artifact, calibration configuration, scoped candidate diagnostics, source artifacts and seven controls |
| Outputs | Calibrated estimate, prediction set and support decision, typed uncertainty, provenance, evidence and limitations; never a parent emission |
| Runtime | Stateless deterministic quality-gated service; no training, fitting, registry, database, external-content traversal or persistence |
| Methods | Conformal, score calibration and selective ensemble are declared enum options; implementation applies deterministic gates to a caller-declared candidate |
| Scope dimensions | Site, platform, disease class and subgroup |
| Coverage gate | Nominal 90%; provisional acceptance envelope 85%-95% |
| Safe failure | Missing/unscoped/low-support/OOD/coverage/error/disparity candidates abstain; unsupported is never converted to negative evidence |
| Interfaces | `POST /m08-07/calibrate`, `POST /m08-07/verify`, `GET /m08-07/schema`; Typer `validate`, `calibrate`, `export-schema`; strict plugin validate/run |
| Claims ceiling | Behavioral contract and deterministic gate evidence only; not authenticated calibration, subgroup validity, biological truth, transportability or clinical use |

## Data and model manifest

All executable fixtures contain synthetic opaque metadata. No patient data, raw spectra, peptide
strings, accessions, sequences, treatment data or external scientific content is read. The
candidate's confidence and diagnostics remain caller-declared; no model weights, posterior fit,
calibration training or probability estimator executes in this package.

## Review and rollback

Owner confirmation is required before freezing any ABI, catalogue, metric ceiling or release
media type. OOD state, support override, subgroup disparity, claim promotion or release exception
requires human review. Recovery is append-only: retain the immutable request/result and publish a
new superseding result after external correction and full replay.

