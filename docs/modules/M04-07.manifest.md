# GLIO-PROTEOGEN-M04-07 module manifest

| Property | Locked value |
| --- | --- |
| Module | `GLIO-PROTEOGEN-M04-07` |
| Title | Unsupported-case and abstention router |
| Component | C04 Proteoform/isoform inference |
| Owner | Computational biology |
| Version | `1.0.0` |
| Safety class | `S2` |
| Evidence gate | `G1` |
| Operation | `route_proteoform_support` |
| Output artifact media type | `application/vnd.glio-proteogen.m04-07+json` |
| Benchmark gate | One warmup plus 25 public calls; mean <=2 s and p95 <=3 s |
| Upstream closure | Full strict M04-04 quality and M04-06 harmonization results plus compact receipts rederived by public helpers from those exact results, with exact result/disposition/profile/configuration/lineage bindings; M04-07 does not reinterpret or mutate the full results |
| Inputs | Seven authorization controls, full strict M04-04/M04-06 results and their exact compact prerequisite receipts, four typed caller declarations, three typed context receipts, one reviewed joint-envelope profile, and one policy |
| Support dimensions | Exactly `assay`, `specimen`, `disease_class`, `quality`, `completeness`, `platform`, `reference`, and `intended_use` |
| Declared states | `observed`, `missing`, and `unknown`; missing and unknown remain distinct indeterminate states |
| Match semantics | One envelope must admit the complete joint declaration; platform and reference require all declared members |
| Output ceiling | Support/abstention disposition, dimension and envelope assessments, fixed abstention reasons, reviewed remediation paths, compact receipts, support, uncertainty, provenance, evidence, limitations, and review state |
| Capacity | Installed 8-dimension, 4-fact, 3-context-receipt, 64-envelope, 64-value-per-fact, exact dependency-bound public M04-06 platform-level and analysis-target, 32-approved-version-per-domain, 8-evidence-per-fact, 514-abstention, 46-result-evidence, 1,000,000-rate-scale, and 4 MiB canonical-request ceilings |
| Graph identifiers | Content-derived `request`, `profile`, `policy`, `envelope`, `specimen`, `disease`, `reference`, `use`, `reason`, `remediation`, `evidence`, `reviewer`, and `route` namespaces, each followed by `.` and exactly 64 lowercase hexadecimal characters; projected platform IDs remain exact upstream `level.<64 lowercase hex>` values |
| Parent target | Exact `protein_rna_discordance` workflow context; no protein-RNA-discordance inference or score |
| Runtime model | Stateless deterministic joint-envelope routing with typed abstention and canonical all-failure remediation; no learner, estimator, database, or external reference runtime |
| Architecture selection | Schema-first batch routing plus quarantine-first safe failure; event log, relational registry, object store, anomaly model, evidence graph, territory-conditioned subtype, and spatial proteotype field are declared not executed |
| API | `POST /v1/modules/M04-07/support-route`; schemas at `GET /v1/contracts/M04-07/{name}/schema` |
| CLI | `proteoform-support route REQUEST`; `proteoform-support export-schema NAME` |
| Schema names | `request`, `output`, `prerequisites`, `quality-receipt`, `harmonization-receipt`, `fact`, `context-receipt`, `profile`, `policy`, `envelope`, `remediation`, `dimension-assessment`, `envelope-assessment`, and `abstention` |
| Evidence data | Small synthetic non-clinical categorical declarations, opaque identifiers, fixed-point summaries, role bindings, and content digests only |
| Claims ceiling | Deterministic synthetic joint-envelope routing regression only; not support-domain, transport, assay, protein/proteoform, biological, protein-RNA-discordance, probability, or clinical validation |
| Dossier source | Authoritative dossier lines 1380-1420; the locked G1 design narrows execution to deterministic joint-envelope routing |

M04-07 owns only support-domain and abstention routing after proteoform/isoform-inference quality assessment
and technical harmonization. It does not parse raw inputs, infer or repair identity, mutate
upstream evidence, select a protein or proteoform, estimate abundance or calibrated probability,
infer protein-RNA discordance or kinase state, fuse omics, recommend treatment, or provide clinical
decision support.

All seven uncertainty dimensions remain not estimable. A critical discrepancy, novel or OOD
state, support override, claim promotion, release exception, or unresolved biological conflict
requires governed external human review.

The locked corpus is `tests/fixtures/m04_07/scenarios.json`. Executable requirement links are in
`docs/traceability/GLIO-PROTEOGEN-M04-07.csv`.
