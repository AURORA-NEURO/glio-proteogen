# GLIO-PROTEOGEN-M03-03 module manifest

| Property | Locked value |
| --- | --- |
| Module | `GLIO-PROTEOGEN-M03-03` |
| Title | Raw-format ingestion and parser |
| Component | C03 Protein inference/ambiguity control |
| Owner | Quality engineering |
| Version | `1.0.0` |
| Safety class | `S2` |
| Evidence gate | `G0` |
| Operation | `ingest_protein_inference_raw_inputs` |
| Upstream closure | Exact genuine public M01-02 identity resolution, M03-01 protocol result, and M03-02 identity/lineage reconciliation result; valid upstream quarantine or abstention propagates before source I/O |
| Inputs | Approved controls; content-addressed source declarations; one governed source directory; spectra/mzML, peptide evidence/mzIdentML, protein-group JSON, ambiguity JSON, complex-activity-input-bundle JSON, exact search-space FASTA components, PSI-MOD OBO, VCF, and GFF3 bytes |
| Search-space components | Exactly governed `canonical`, `decoy`, `conditional_isoform`, `variant`, and `contaminant` FASTA roles bound to M03-01 |
| V1 format exclusion | mzTab and mzTab-M are absent from the supported-format vocabulary and are never silently coerced to another profile |
| Filesystem boundary | Request-declared exact portable basenames beneath one source directory; regular non-reparse files only; no traversal, directory names, symlinks, junctions, reparse points, or alternate data streams; authorize and validate the complete mapping before I/O; read each admitted file exactly once |
| Capacity | At most 64 declared sources; the sixty-fifth source is rejected before any source read |
| Output ceiling | Immutable content-addressed admission capsule and typed parse diagnostics with support, uncertainty, provenance, evidence, limitations, disposition, and review state |
| Parent target | `complex_activity` workflow support only; no complex-activity inference or score |
| Runtime model | Deterministic bounded admission, decompression, strict profile parsing, cross-reference closure, upstream binding, and context-coherence checks; no protein inference, abundance estimation, activity model, subtype model, or clinical decision support |
| CLI | `protein-inference-raw ingest REQUEST SOURCE_DIRECTORY`; `protein-inference-raw export-schema NAME` |
| Schema API | Exact installed schemas through `GET /v1/contracts/M03-03/{name}/schema`; deliberately no raw-ingestion POST route |
| Schema URN | `urn:aurora-neuro:glio-proteogen:GLIO-PROTEOGEN-M03-03:1.0.0:{name}` |
| Evidence data | Small synthetic, non-clinical bytes, opaque identifiers, categorical controls, and content digests only |
| Claims ceiling | Reproducible structural admission of a locked synthetic source capsule; not proof of identity, laboratory execution, observed peptide or protein, protein inference, activity, biological validity, or clinical readiness |

M03-03 owns the source-admission boundary beneath protein inference and ambiguity control. It does
not mutate M01-02, M03-01, or M03-02; rewrite an identifier; choose between biological
disagreements; repair a malformed reference; infer a peptide, protein, proteoform, abundance,
complex activity, subtype, proteotype, or kinase state; fuse omics; recommend treatment; or provide
clinical decision support.

The locked corpus is `tests/fixtures/m03_03/scenarios.json`. Executable requirement links are in
`docs/traceability/GLIO-PROTEOGEN-M03-03.csv`.
