# GLIO-PROTEOGEN-M03-07 module manifest

| Property | Locked value |
| --- | --- |
| Module | `GLIO-PROTEOGEN-M03-07` |
| Title | Protein-inference unsupported-case and abstention router |
| Component | C03 Protein inference/ambiguity control |
| Owner | Scientific engineering |
| Version | `1.0.0` |
| Safety class | `S2` |
| Evidence gate | `G1` |
| Operation | `route_protein_inference_support` |
| Upstream closure | Full strict M03-04 quality and M03-06 harmonization results plus compact receipts rederived by public helpers from those exact results, with exact result/disposition/profile/configuration/lineage bindings; M03-07 does not reinterpret or mutate the full results |
| Inputs | Seven authorization controls, full strict M03-04/M03-06 results and their exact compact prerequisite receipts, four typed caller declarations, three typed context receipts, one reviewed joint-envelope profile, and one policy |
| Support dimensions | Exactly `assay`, `specimen`, `disease_class`, `quality`, `completeness`, `platform`, `reference`, and `intended_use` |
| Declared states | `observed`, `missing`, and `unknown`; missing and unknown remain distinct indeterminate states |
| Match semantics | One envelope must admit the complete joint declaration; platform and reference require all declared members |
| Output ceiling | Support/abstention disposition, dimension and envelope assessments, fixed abstention reasons, reviewed remediation paths, compact receipts, support, uncertainty, provenance, evidence, limitations, and review state |
| Capacity | Installed 8-dimension, 4-fact, 3-context-receipt, 64-envelope, 64-value-per-fact, 512-platform-level, 32-approved-version-per-domain, 8-evidence-per-fact, 514-abstention, 46-result-evidence, 1,000,000-rate-scale, and 4 MiB canonical-request ceilings |
| Graph identifiers | Content-derived `request`, `profile`, `policy`, `envelope`, `specimen`, `disease`, `reference`, `use`, `reason`, `remediation`, `evidence`, `reviewer`, and `route` namespaces, each followed by `.` and exactly 64 lowercase hexadecimal characters; projected platform IDs remain exact upstream `level.<64 lowercase hex>` values |
| Parent target | Exact `complex_activity` workflow context; no complex-activity inference or score |
| Runtime model | Stateless deterministic joint-envelope routing with typed abstention and canonical all-failure remediation; no learner, estimator, database, or external reference runtime |
| API | `POST /v1/modules/M03-07/support-route`; schemas at `GET /v1/contracts/M03-07/{name}/schema` |
| CLI | `protein-inference-support route REQUEST`; `protein-inference-support export-schema NAME` |
| Schema names | `request`, `output`, `prerequisites`, `quality-receipt`, `harmonization-receipt`, `fact`, `context-receipt`, `profile`, `policy`, `envelope`, `remediation`, `dimension-assessment`, `envelope-assessment`, and `abstention` |
| Evidence data | Small synthetic non-clinical categorical declarations, opaque identifiers, fixed-point summaries, role bindings, and content digests only |
| Claims ceiling | Deterministic synthetic joint-envelope routing regression only; not support-domain, transport, assay, protein/proteoform, biological, complex-activity, probability, or clinical validation |

M03-07 owns only support-domain and abstention routing after protein-inference quality assessment
and technical harmonization. It does not parse raw inputs, infer or repair identity, mutate
upstream evidence, select a protein or proteoform, estimate abundance or calibrated probability,
infer complex activity or kinase state, fuse omics, recommend treatment, or provide clinical
decision support.

The locked corpus is `tests/fixtures/m03_07/scenarios.json`. Executable requirement links are in
`docs/traceability/GLIO-PROTEOGEN-M03-07.csv`.
