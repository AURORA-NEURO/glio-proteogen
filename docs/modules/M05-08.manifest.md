# GLIO-PROTEOGEN-M05-08 — provenance and release packaging

Status: provisional local implementation; no ABI freeze or publication claim.

Authoritative dossier slice: lines 1784–1824 of `GLIO-PROTEOGEN_240_Module_Dossier.md`.

Dossier SHA-256: `0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`

## Responsibility and boundary

M05-08 owns immutable provenance and release packaging beneath the PTM localization
interface. The implementation records software/reference versions, checksums,
transformations, quality decisions, support and reproducibility evidence, and emits a
canonical signed package only after an injected allow-listed verifier succeeds.

It does not infer identity or consent, mutate upstream evidence, convert missing or
unsupported evidence into negative findings, perform generic all-omics fusion, make a
treatment recommendation, or own KINOPHOS kinase activity.

## Provisional interface

The strict request, manifest, policy, artifact, signature, result and verification
models live under `src/glio_proteogen/contracts/m05_08`. Their `0.1.0-provisional`
version, byte limits, fixture values and package member names are review scaffolding,
not a frozen production ABI. All ingress uses duplicate-key-safe bounded JSON parsing.

The runtime package is deterministic canonical USTAR with these provisional members:

```text
manifest/policy.json
manifest/reproducibility.json
manifest/signature.json
parent/variant-peptide.json
```

No private key or signing secret is accepted by the module. Without an injected
allow-listed verifier, the service returns a quarantined result and no package bytes.

## Evidence map

| Dossier requirement | Implementation evidence |
| --- | --- |
| Contract/schema G0 | `contracts/m05_08/v1.py`, `schema.py`, hardening tests |
| Immutable provenance and checksums | manifest/artifact/transformation closure validators and canonical digests |
| Safe failure and review routing | quarantine result invariants and missing-verifier/unsupported-support tests |
| Reproducibility | canonical USTAR build, package inspection and tamper tests |
| Service/plugin boundary | `service.py`, `plugin.py`, parse-once token tests |
| API/CLI parity | `api.py`, `cli.py`, integration parity tests |
| Locked evaluation/benchmark | `evals/m05_08/run.py`, `evals/m05_08/benchmark.py`, evidence reports |

Required owner review remains open for field names, signing semantics, fixture values,
limits, and release authority.
