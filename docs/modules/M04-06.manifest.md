# GLIO-PROTEOGEN-M04-06 module manifest

| Property | Locked value |
| --- | --- |
| Module | `GLIO-PROTEOGEN-M04-06` |
| Title | Harmonization and normalization engine |
| Component | C04 Proteoform/isoform inference |
| Owner | Scientific engineering |
| Version | `1.0.0` |
| Safety class | `S2` |
| Evidence gate | `G1` |
| Operation | `harmonize_proteoform_analysis` |
| Upstream closure | Full strict replay of a public M04-05 result plus its exact content-addressed target receipt; executable evidence constructs the genuine public M04-01 -> M04-02 -> M04-03 -> M04-04 -> M04-05 chain |
| Inputs | Seven authorization controls preserved verbatim from M04-05, complete M04-05 result and target receipt, fixed-point support ledger, reviewed eight-stage profile, protected invariants, and harmonization policy |
| Technical factors | Exactly `platform`, `batch`, `laboratory`, `build`, `depth`, `purity`, `composition`, and `preanalytic` |
| Arithmetic | Exact signed integer lower-median paired shifts on support coordinates bounded to the 1,000,000 scale; declared sequential stage application, reviewed shift cap, and bounded coordinate clamp; no binary floating point, abundance interpretation, learned model, or calibrated probability |
| Observation states | `observed`, `missing`, `censored`, `not_applicable`, and `unsupported`; observed zero is distinct from every non-observed state |
| Artifact firewall | Only M04-05-retained targets may train or receive a correction; review and excluded targets never contribute to estimates and receive no adjusted coordinate |
| Protected invariants | Held-out technical residual reduction plus `support_direction`, `support_rank`, and `composition_fraction` replay |
| Output ceiling | Owned analytical outputs are only the immutable fixed-point harmonized analysis object and transformation manifest; the strict result envelope additionally carries replay diagnostics, findings, support, uncertainty, provenance, evidence, limitations, disposition, and review state |
| Acceptance | All eight technical stages replay exactly; every held-out residual meets its reviewed criterion; all three protected invariant kinds pass; finite synthetic corpus only |
| Capacity | The exact embedded M04-05 receipt ceiling remains 64 targets. Installed M04-06 processing is bounded to 8 stages, 32 targets, 32 observations, 32 levels per factor, 256 level shifts, 32 estimation anchors per stage, 32 validation anchors per stage, 256 invariants, 32 target references per invariant, 8 evidence references per observation, 256 applied adjustments, 16 profiles, 32 approved versions per domain, 16 result evidence references, 14 findings, the 1,000,000 coordinate scale, the exact 2,000,000 post-validation-residual ceiling, and a 4 MiB canonical request. A cleared 33- through 64-target M04-05 result is preserved in full and produces typed M04-06 abstention without a support ledger, analysis, manifest, or truncation. |
| Parent target | Exact `protein_rna_discordance` workflow context; no protein-RNA discordance inference or score |
| Runtime model | Stateless full-result replay and receipt/ledger closure, artifact firewall, exact lower-median fixed-point normalization, held-out technical validation, protected-invariant replay, and deterministic safe-failure precedence |
| CLI | `proteoform-harmonization harmonize REQUEST`; `proteoform-harmonization export-schema NAME` |
| Harmonization API | `POST /v1/modules/M04-06/harmonization` |
| Schema API | `GET /v1/contracts/M04-06/{name}/schema` |
| Schema URN | `urn:aurora-neuro:glio-proteogen:GLIO-PROTEOGEN-M04-06:1.0.0:{name}` |
| Evidence data | Small synthetic non-clinical integer coordinates, categorical states, opaque identifiers, role bindings, and content digests only |
| Graph identifiers | Content-derived `request`, `policy`, `profile`, `ledger`, `target`, `anchor`, `group`, `level`, `invariant`, `stage`, `reviewer`, and owned `evidence` namespaces, each followed by `.` and exactly 64 lowercase hexadecimal characters; output references reuse exact upstream `target.<digest>` identifiers and owned evidence uses strict lowercase `type/subtype` media types |
| Claims ceiling | Reproducible technical support-coordinate harmonization under the locked synthetic profile; not abundance, probability, protein/proteoform identity, biological truth, protein-RNA discordance, assay qualification, or clinical readiness |

M04-06 owns only deterministic technical harmonization of caller-declared support coordinates for
the M04 proteoform graph. It does not mutate M04-05 evidence; inspect raw spectra or peptide
sequences; infer a peptide, protein, protein group, proteoform, abundance, complex activity,
subtype, proteotype, or kinase state; authenticate an external issuer; fit a learned model; fuse
omics; recommend treatment; or provide clinical decision support.

The locked corpus is `tests/fixtures/m04_06/scenarios.json`. Executable requirement links are in
`docs/traceability/GLIO-PROTEOGEN-M04-06.csv`.
