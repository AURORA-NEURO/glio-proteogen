# GLIO-PROTEOGEN-M27-06 — Security and access-control assessment

| Field | Locked implementation value |
| --- | --- |
| Module | `GLIO-PROTEOGEN-M27-06` |
| Operation | `evaluate_complex_activity_security_access` |
| ABI | `0.1.0-provisional` (dossier behavioral brief only) |
| Parent | `complex activity` |
| Owner / safety / gate | Platform engineering / S3 / G4 |
| Input | Caller-declared M27-05 telemetry artifact; content-free security metadata |
| Output | Access decision, security posture, audit event, findings, evidence, and safe failure |
| Interfaces | FastAPI schema/validate/evaluate/verify; Typer export-schema/validate/evaluate/verify |
| Schemas | 8 strict Draft 2020-12 schemas under `urn:aurora-neuro:glio-proteogen:GLIO-PROTEOGEN-M27-06:0.1.0-provisional` |

The implementation performs a seven-control authorization preflight before reading upstream
metadata. It evaluates only caller-declared access markers (`deny` and `threat`), preserves
all eight security controls, and binds request/result digests for deterministic replay. Missing
consent and unsupported upstream media types abstain without security traversal. It never infers
proteins, proteoforms, isoforms, glioma biology, kinase activity, treatment, identity, or consent.
