# GLIO-PROTEOGEN-M04-08 module manifest

| Property | Locked value |
| --- | --- |
| Module | `GLIO-PROTEOGEN-M04-08` |
| Title | Provenance and release packaging |
| Component | C04 Proteoform/isoform inference |
| Owner | Bioinformatics |
| Version | `1.0.0` |
| Safety class | `S2` |
| Evidence gate | `G1` |
| Operation | `package_proteoform_release` |
| Inputs | Authorized M04-01..M04-07 result artifacts, parent handoff, approved release policy, software/reference versions, reproducibility evidence, external signature statement, and injected verifier |
| Output ceiling | Signed release package descriptor and reproducibility manifest with typed support, uncertainty, provenance, evidence, limitations, and review state |
| Parent target | `protein_rna_discordance` workflow support only; no discordance inference |
| Archive format | Deterministic canonical USTAR |
| Archive inventory | Exactly eight caller artifacts plus generated manifest plus generated signature-verification receipt |
| Maximum artifact shape | 32 MiB per caller artifact, 64 MiB caller total, 72 MiB package |
| Maximum metadata shape | 64 software-version and 64 reference-version records |
| Reproduction mode | Exact bytes; no undeclared numerical tolerance |
| Signature semantics | External statement plus injected verifier; no signing, key custody, signer identity, certificate, or release-authority inference |
| M04-07 state | Public ABI not frozen; executable adapter sealed and genuine E2E prohibited until final checkpoint |
| HTTP surface | Schema-only until the final executable boundary is integrated |
| Runtime model | Stateless deterministic packager; no learned model, event store, database, key manager, or external lookup |
| Claims ceiling | Deterministic synthetic package/replay regression only; not scientific reproducibility, cryptographic assurance, supply-chain, assay, biological, clinical, or treatment validation |

The owned schema spine exports request, output, policy, artifact, manifest, verification, signature,
stage-provenance, and reproduction-evidence schemas. The final M04-07 class/import/media type,
identifier convention, disposition vocabulary, receipt access paths, and genuine builder are not
represented by guessed public symbols. The locked evaluator uses synthetic stage objects only and
must report `genuine_e2e_executed=false` and `synthetic_chain_executed=true`; if the M04-07 binding
is unavailable, `M0408DependencyUnavailableError` is raised rather than treating an unresolved
dependency as an executable chain. Locked inventory lives under `tests/fixtures/m04_08`.
