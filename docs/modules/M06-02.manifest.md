# GLIO-PROTEOGEN-M06-02 — representation and feature constructor

Status: provisional local implementation; no ABI freeze or publication claim.

Authoritative dossier slice: lines 1880–1920 of `GLIO-PROTEOGEN_240_Module_Dossier.md`.

Dossier SHA-256: `0a6b200cbe073db13a4bcf315edc23ab97edfe6f500bc7ea2785f5e1c70da181`

## Responsibility and boundary

M06-02 owns versioned analysis representation and feature lineage beneath Protein
abundance model. The implementation preserves caller-declared feature values,
records leakage-safe lineage and provenance, and emits explicit masks and abstention
for missing, not-applicable or unsupported features.

It does not infer identity or consent, mutate upstream evidence, relabel another
module's output, perform generic all-omics fusion, recommend treatment, or own
KINOPHOS kinase activity.

## Provisional interface

The request, feature, lineage, mask, covariate, result and replay-verification
models live under `src/glio_proteogen/contracts/m06_02`. The
`0.1.0-provisional` version, field names, limits and feature catalogue are review
scaffolding, not a frozen production ABI. All ingress uses bounded duplicate-key-safe
JSON parsing.

The runtime emits canonical JSON result bytes. No model weights, external content
traversal, private key or signing authority is accepted by this module.

## Evidence map

| Dossier requirement | Implementation evidence |
| --- | --- |
| Contract/schema G0 | `contracts/m06_02/v1.py`, `schema.py`, hardening tests |
| Deterministic representation | `engine.py`, canonical digest and replay tests |
| Leakage-safe lineage | `FeatureLineageStep`, provenance construction, lifecycle tests |
| Explicit masks and abstention | `_masks`, `_support_status`, safe-failure tests |
| Service/plugin boundary | `service.py`, `plugin.py`, parse-once token tests |
| API/CLI parity | `api.py`, `cli.py`, integration parity tests |
| Evaluation/benchmark/package | `evals/m06_02`, benchmark wrapper and evidence reports |

Required owner review remains open for the learned/mechanistic feature catalogue,
calibration, limits, and release authority.
